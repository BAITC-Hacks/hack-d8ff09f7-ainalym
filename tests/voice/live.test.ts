import { describe, it } from "vitest";

describe("live voice gates", () => {
  it.skip("UNVERIFIED: Russian microphone round-trip through Realtime, what_changed, persisted ledger, and UI state", () => {});
  it.skip("UNVERIFIED: browser stop() after recommend_for keeps the review task visible in the UI", () => {});
});
