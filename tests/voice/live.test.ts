import { describe, it } from "vitest";

describe("live voice gates", () => {
  it.skip("UNVERIFIED: Russian microphone round-trip through Realtime, what_changed, persisted ledger, and UI state", () => {});
  it.skip("UNVERIFIED: recommend_for creates one persisted proposal through the L1/L2 calculation route", () => {});
  it.skip("UNVERIFIED: a voice-created review task remains readable after stop()", () => {});
});
