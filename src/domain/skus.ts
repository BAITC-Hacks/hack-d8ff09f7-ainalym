// Seam (L2b implements): one SKU with its series, forecast, recommendation, in-transit and agent timeline.
export async function skuView(_code_1c: string): Promise<Record<string, unknown> | null> {
  return null;
}
