# ORDERS-1 closeout — «Заказы» (Fable v2 language)

- **Page:** `/v2/orders` (`src/app/(v2)/v2/orders/`: `page.tsx`, `OrdersView.tsx`, `pipeline.ts`, `orders.module.css`) built on the Fable shell primitives (`PageHead`, `Kpis`, `Section`, `Card`, `Pill`) and `styles/v2/tokens.css`; the v2 layout/Shell and nav are untouched.
- **Shows:** pipeline list of every order with a six-step rail (черновик → утверждён → письмо подготовлено → ответ поставщика → в пути → получен) from real states only; system orders from `/api/orders` plus the partner's in-transit orders from the 1С «Товар в пути» reports (grouped by header, via the new read-only `GET /api/sources`). Right rail per selected order: header text, supplier reply text (or «ответа пока нет»), document checklist with «получен / не получен / не требуется», 30/70 payouts from `/api/money`, partner files with data dates and load time.
- **Real today:** draft/approved/exported state; supplier reply (world feed `supplier_reply`, defensive read); prepared letter (ledger row that says «письмо», or implied by a reply); IEK header dates «поступление до dd.mm.yyyy» → «поступление до dd.mm» + days until receipt; obligations 30/70.
- **Absent today (shown as not reached, never invented):** no system order has a real arrival date (`arrival_at`/`arrives_by`/`arrival_by` are read defensively; plan `eta` is labelled «расчёт, поставщиком не подтверждён»); SE «СЭ 24.09» carries no date → «в пути» without a date; the ETL `expected_at` placeholder is ignored; «получен» state never lands because no source reports receipt.
- **Customs line:** «граница/таможня ~2–3 дн (регламент)» as a secondary policy line only for IEK rows marked «РФ» in the partner header and for system IEK orders once in transit; never for SE, never inside a date.
- **Tests:** `tests/ui/v2_orders_pipeline.test.ts` (7 cases: rail mapping, days-until, customs only for IEK, placeholder date ignored, letter detection) — `npm run check` passed=258 failed=0; `npx tsc --noEmit` clean; `npm run build` passes (`/v2/orders`, `/api/sources`).
- **Evidence:** `docs/evidence/v2/orders/orders_desktop.png` (+ `_full`) at 1440×900 from `tests/e2e/v2_orders.spec.ts` (`--config tests/e2e/v2_orders.config.ts`), which also asserts that no technical term (state_version, JSON, HTTP, ETL, provider, undefined, null) is visible.
- **Seeded state for the screenshot (local worktree DB only):** one calc run, two proposals approved (IEK draft, SE approved + simulated supplier reply). The supplier-letter draft returned «provider unavailable» in rules mode, so the letter step on the SE order is inferred from the reply.
- **Deviations:** started under Opus A per the original brief, moved to v2 on the root redirect; nothing left under `(opus_a)`. Ledger kind `order_drafted` is shared by the draft order and the letter — distinguished by summary text, noted for the engine lane.
- **Gate: GREEN** (unverified surface: real arrival-date field name from the engine lane; phone viewport not screenshotted).
