# L4c integration notes

- L4b/L4a: import `AssistantPanel` from `@/components/assistant`; props `scope?: {org_id, supplier_id?, code_1c?}`, `onClose?`, `variant?: "panel" | "page"`. `/assistant` is the entry for L4a's navigation. Default org is `partner` from the world fixtures; the runtime must seed that organization or pass the active organization explicitly. Bare ETL leaves organization empty; the actual tool returns `Unknown organization scope` (403).
- L5 resolved: successful `ainalym:voice-tool-result` events now become deduplicated cards during the active session. A UI regression verifies the seam. Actual spoken result remains externally-unverified until the root microphone run.
- L1: `/api/modes`, `/api/state`, `/api/agent/runs/:id` remain absent at this checkpoint. Связи additionally needs mode data-source metadata `{name,supplier_id,as_of,anonymised,rows?}`; missing metadata is disclosed. Expose active `org_id` on an API so the assistant page can select the real organization without a hardcoded deployment assumption. Normalise `axes.ai` to §5 enum values.
- L4a: `WorldFeed` has landed, but expects `{events}` while L6 serves `{rows}`; its compose lacks structured `qty`/`delta`/`unit_cost`. Console keeps working controls in owned paths and shares shell primitives. Extract reusable controls after reconciling these boundaries; do not replace them with a non-working integration. The domain preserves SKU currency during price updates.
- L8 health interception resolved on main: the actual health route now supplies provider and voice capability fields.
- Reframe takes precedence: four replenishment tools; 1С title `Экспорт для 1С (файл)` comes from modes. Historical diagnostic meta remains verbatim L6 step 6 per the explicit gate; it does not claim a current connector.
- Tier 2 shell integration seam: `NotificationsBell` and `SearchResults({query,onNavigate})` live in `@/components/lists`. L4a owns mounting the bell globally and adding API results to its existing ⌘K palette. Existing shell g-chords remain authoritative.
