# OPUS-B closeout — Opus 5.5, branch `lane/opus_b`, routes `/opus_b/{today,replenishment,skus/[code]}` (D-H50)
1. **Today** (`/api/today`, `/api/queue`, `/api/proposals/:id` for the bound version, `/api/agent/ledger`, POST `/api/proposals/:id/approve|reject`, POST `/api/calc/run`): Ramp Home: a 56 px heading, a four-number strip (636 SKU at risk in red, 67,8 млн ₸ waiting with an SE/IEK split bar, obligations, agent ratio bar), decision cards with supplier marks, the single chartreuse «Подготовить заказ» and version chip, beige alert rows for agent tasks, a ledger feed with «сам / ждёт вас» pills, and a rail of risk cover meters and money. Taste: one glance ranks the risk, then the money, then the decision, in that order, and colour is spent only where it carries meaning.
2. **Replenishment** (`/api/recommendations[?supplier]`, `/api/skus?supplier=SE` for unit cost, POST `/api/recommendations/:id/adjust`, `/api/calc/run`): the Bills table: tabs and urgency filters with counts, a sticky supplier header (quantity, cost or «себестоимость не задана», price-known share), two-line cells, and urgency shown as pill + cover line + 3 px row edge. Enter opens the rationale as a **calculation tape**: demand × season/growth = forecast + safety − on hand − in transit = need → MOQ → qty, in the engine's own numbers, plus seasonality, excluded documents, compensated stockout months, sources and inline adjust. Taste: the quantity reads as the arithmetic that produced it, not as prose.
3. **SKU card** (`/api/skus/:code`): Ramp Banking: a hand-made SVG with 24 months of bars and 6 forecast months (a full-month-rate dashed plum line), stockout months as labelled beige bands with hatched bars, excluded one-offs as hatched red caps, the order-horizon tag and a dark tooltip. The right rail holds the recommendation as a vertical receipt, sources with as-of dates, in-transit POs with ETA and the agent journal. Taste: the chart is the evidence and the rail is the verdict.
- **States** (evidence `today-state-*.png`, `replenishment-state-adjust-unavailable.png`): loading skeleton (fixed heights); empty; unavailable; stale = a **real server 409** (approve re-sent with version 999, nothing approved) → «Данные обновились — обновите». The adjust route is not on main, so the **real 404** shows «Правка на сервере пока недоступна — количество не изменено». Unavailable and empty were captured by intercepting the read route. The SKU card handles 404 and error.
- **Keyboard** (`docs/evidence/opus_b/keyboard.txt`, Playwright-driven): Tab order rail → search → header → strip → decisions; ⌘K focuses the search, ↓ Enter opens a SKU; table uses a roving tabindex with ↑↓/j/k, Enter opens the rationale, Esc closes it and focus returns to the row; chart ← ← goes «сен 2026» → «июл 2026» in the live region; visible 2 px focus ring; phone targets 44 px.
- **Screenshots** `docs/evidence/opus_b/`: `today-{desktop,phone}[-full].png`, `today-desktop-{focus,search}.png`, `replenishment-{desktop,phone}[-expanded].png`, `sku-{desktop,phone}[-full].png`, `sku-desktop-chart-keys.png`; 0 console errors. Re-run: `node docs/evidence/opus_b/shoot.mjs` with the dev server on :3120.
- **Checks:** `npx tsc --noEmit` exit 0; `npm run build` exit 0 (routes `/opus_b`, `/opus_b/today`, `/opus_b/replenishment`, `/opus_b/skus/[code]` listed). Dev server stopped.
- **Commits:** a459a1a (shell + Today), d9fb9b2 (Today colour), 43c03fd (Replenishment), 512dab6 (SKU card); tip = the evidence + closeout commit that follows.
- **Ramp viewed (Read tool):** Bill_pay/00.26.25, Bill_pay/00.29.14, Bill_pay/00.31.51, banking/00.35.26, banking/00.37.00, accounting/00.42.01, card_payments/00.18.08, Pay_intake/00.46.07, landings_snapshots/21.58.24 (all `CleanShot 2026-09-1x at …@2x.png`).
- **Deviations:**
  - Commit trailer is `Claude Opus 5.5 (1M context)`, not the briefed `Claude Fable 5.1`, because Opus wrote this code.
  - Screens 2 and 3 were built by two Opus forks inside this worktree, each with its own files; I integrated and reviewed them and made one chart fix.
  - Playwright is installed with `--no-save`, so package.json is untouched. The evidence runner is `docs/evidence/opus_b/shoot.mjs` rather than `tests/e2e/opus_b.spec.ts`, so tsc does not depend on `@playwright/test` before the e2e lane merges it.
  - Only tokens under `[data-ob]` were added; global tokens are untouched.
- **Unverified:** a successful adjust (the route is missing); the approve happy path, which I did not click to avoid mutating the demo DB; sticky headers during very long scrolls.
Gate: YELLOW (all three screens are built, green on build and tsc, with evidence; adjust depends on the L2a route).
