# L1 skeleton closeout — replenishment case

BEFORE: schema v0, SQLite client, seam stubs, partner XLSX; no app routes, check runner, reset, ledger, or reviewer dev page.
AFTER: typed transactional repositories at src/db/repo/index.ts:44; additive indexes in src/db/schema.sql; ETL reset in scripts/demo_reset.mjs:21 and src/server/fixtures.ts:4; calc handoff at src/server/calc.ts:7; persisted run/action ledger at src/server/ledger.ts:9 and event inbox at src/server/pipeline.ts:9; reviewer pulse at src/app/api/today/route.ts:8; dev page at src/app/skeleton/page.tsx:1; real storage and API types at docs/CONTRACTS.md:14 and src/server/contracts.ts:1.

Owned files changed: package.json, vitest.config.ts; scripts/check.mjs, scripts/vitest_reporter.mjs, scripts/demo_reset.mjs; src/db/client.ts, src/db/schema.sql, src/db/repo/index.ts; src/server/calc.ts, context.ts, contracts.ts, fixtures.ts, http.ts, ledger.ts, pipeline.ts, scheduler.ts; src/instrumentation.ts; src/app/skeleton/page.tsx; docs/CONTRACTS.md, docs/agent_handoffs/L1_CHECKPOINT.md, docs/agent_handoffs/L1_SCHEMA_NOTE.md, this file.
L1 route files changed: src/app/api/health/route.ts, state/route.ts, modes/route.ts, demo/reset/route.ts, demo/example/route.ts, calc/run/route.ts, calc/runs/route.ts, calc/runs/[id]/route.ts, recommendations/route.ts, skus/route.ts, skus/[code]/route.ts, params/route.ts, money/route.ts, today/route.ts, queue/route.ts, agent/ledger/route.ts, agent/runs/route.ts, agent/runs/[id]/route.ts, agent/tick/route.ts.
Tests changed: tests/skeleton/smoke.test.ts, repository_ledger.test.ts, pipeline.test.ts, reset.test.ts. No L1 dependency added; L9's merged ETL added xlsx.

Tables: organization(id); supplier(id); sku(code_1c); sales_line(id); sales_month(code_1c,ym); stock_month(code_1c,ym); in_transit(id); seasonality(supplier_id,year,month); season_index(supplier_id,month); outlier_doc(id); forecast(id); calc_run(id); recommendation(id); purchase_order(id); purchase_order_line(id); proposal(id); approval(id); task(id); obligation(id); payment(id,payment_ref unique); world_event(id,(org_id,source_id) unique); agent_run(id); agent_action(id,idempotency_key unique); decision_record(id); ledger_peer_record(id,(peer,external_identity) unique); state_version(singleton n).
Additive indexes: world_event(org_id,state,seq), world_event(code_1c,state), agent_run(org_id,started_at), agent_action(org_id,at), agent_action(po_id,at). No renamed columns; client.ts exports preserved.

Routes/status: GET health 200/503; state,modes,money,today,queue,recommendations,skus,agent ledger/runs,calc runs 200; unknown SKU/run 404; POST demo/reset 200/503; demo/example and calc/run 200/400/500; PUT params 202/400/404; POST agent/tick 200/500. All L1 JSON errors use {ok:false,code,message,field?}. Live /api/health is currently intercepted by L8 middleware and returns guard fields only; its L1 handler has the complete documented shape.

Gate evidence: npm install && npm run check → check: passed=60 failed=2 skipped=0 externally-unverified=0. The two failures are tests/domain/a_money.test.ts (Number() in src/domain/cashflow.ts) and tests/domain/b_money_orders.test.ts (recomputeAffected on SE-1 lacks sales source). npm run check skeleton → passed=7 failed=0 skipped=0 externally-unverified=0. The runner's UNVERIFIED note accounting was exercised with a temporary ctx.skip probe: skipped=1 externally-unverified=1; the probe was removed.
Gate evidence: npm run demo:reset twice → identical counts: organization=1, supplier=2, sku=3909, sales_line=248915, sales_month=99634, stock_month=117282, in_transit=313, world_event=45, recommendation=0, agent_action=0. POST /api/demo/example with AI_PROVIDER=rules → 560 calculated SKUs, 291 recommendations, one supplier proposal, excluded missing_sales=150 and missing_stock=14; /api/today queue_count=1, pulse.agents.ratio=0.99825; /api/queue one SE order; /api/agent/ledger has run_id, kind and sources; POST /api/agent/tick → processed=0; /api/money has cash/committed/out arrays; /skeleton renders organization, mode banner and counts.
Gate evidence: npm run build GREEN, npx tsc --noEmit clean. Live GET /api/health on port 3002 returned {ok:true,demo_guard:"off",remaining_daily_budget:null} from src/middleware.ts rather than the L1 route. Port 3000 belonged to the separate world worktree, so live checks used 3002.

Edge cases: a repeated source_id does not add world_event or bump state_version; a foreign org event raises 404; duplicate ledger action key does not write; reset starts scripted events without processing. The partner SKU catalog is the union of exports; the SE calculation explicitly reports rows without usable sales or known stock rather than inventing inputs. .env.local was read only through process.env and was never committed or printed. No model or bank/1C network calls were made by L1.

Shared requests: L2a fix cashflow coercion test and implement/verify todayView and queueView; L2b resolve the SE-1 recompute test fixture or missing-source expectation. L3 use startRun/recordAction/finishRun and onEvent semantics; L4 use /api/today, /api/queue, /api/modes; L5 voice tools use the same queue/ledger; L6 world play should call onEvent and expose a one-step play function for scheduler autoplay. L8 let /api/health fall through middleware to the L1 route, which now includes demo guard fields.

Unverified: full check GREEN after L2 fixes; health route end-to-end after L8 fix; L6 world play/autoplay integration after its merge; voice and hosted demo remain other lanes' gates.
Gate: RED
tip: 5baa6a6
