# FABLE_UI_1 closeout — /v2/today + /v2/replenishment (branch `lane/fable_ui_1`, worktree `../worktrees/fable_ui_1`)

**Today / Pulse (`/v2/today`).** Decision: Ramp-Home language — warm greige canvas, one 56 px «Сегодня» over a dated eyebrow, a four-cell number strip (stock value + cost-known bar, stockout risk → link, decisions waiting, agents-auto + ratio bar), decision cards with the single chartreuse action, a right rail (stockout top-5 with urgency pills, agents in background, obligations), truth labels in the footer. Routes: `/api/today` (one request, owned by the shell and shared via `useTodaySnapshot`), `/api/queue`, `/api/proposals/:id` (version per card), `POST /api/proposals/:id/approve|reject` `{proposal_version}`. Task-kind queue items get a card with no invented action («маршрут /api/tasks не подключён»). States: populated · empty · unavailable (503) · stale (409 → «Данные обновились…» + Обновить) · offline (shell status). Keyboard: Tab order, visible focus, `⌘K`/`/` search, Enter/Space on buttons, `<details>` sources per card. Screenshots: `docs/evidence/v2/fable_ui_1/today_{populated,empty,unavailable,stale409,offline}_{1440x900,390x844}.png`.

**Replenishment (`/v2/replenishment`).** Decision: the Ramp Bills table — supplier tabs with counts, urgency chips, `/` filter, two-line cells (name + code · MOQ · cover days; stock with as-of date; qty with «по расчёту / скорректировано / черновик»), right-aligned money («не задана» for IEK), and an expandable rationale row: the formula as a ledger (прогноз + запас − остаток − в пути = потребность → кратность → qty) with sources and as-of dates, monthly-forecast bars, seasonality sparkline, stockout/one-off pills, `rationale_ru`, and a quantity draft. Routes: `/api/recommendations`, `/api/proposals?state=needs_review`, `/api/calc/runs`, `/api/skus?supplier=SE` (unit cost — rows don't carry it), `POST /api/proposals/:id/approve` `{proposal_version, adjustments}`; `POST /api/calc/run` from the empty state. States: populated · expanded · filtered-empty · empty (no run) · unavailable · stale-409 · offline (shell). Keyboard proof (asserted in `tests/ui_v2/screens.spec.ts`): `j`/`k` move row focus, Enter/Space toggle `aria-expanded`, Esc collapses and returns focus, `/` focuses the filter. Screenshots: `docs/evidence/v2/fable_ui_1/replenishment_{populated,expanded,filtered_empty,empty,unavailable,stale409}_{1440x900,390x844}.png`, `replenishment_adjust_{before,after}_1440x900.png`.

**Adjust/approve verified end to end (root note 10:0xZ).** `POST /api/recommendations/:id/adjust` → **404** (route in CONTRACTS is not implemented; rows carry no `version`). The real version-bound path is `POST /api/proposals/:id/approve` with `{proposal_version, adjustments:[{code_1c, qty}]}` → 200, version 1→2, PO draft, `qty_adjusted` written; repeat with the old version → **409 `stale`**; ledger row «Одобрено предложение PR-…». Proved by curl (09:55Z) and by the UI twice: `adjust_approve_proof.json` (Playwright run 10:16Z, 010400444_ 132→135) and `adjust_approve_proof_run2.json` (10:38Z, captured by curl after the proof test timed out on the post-approve calc rerun — the approve itself landed).

**Checks.** `npx tsc --noEmit` clean · `npm run build` OK (routes `/v2/today`, `/v2/replenishment`) · `npm run check` → `passed=205 failed=0 skipped=6 externally-unverified=4` (0 failed; main at e4486c9 has 250 because it is ahead of my base) · Playwright (`npx playwright test -c tests/ui_v2/playwright.config.ts`, against `next start` on :3211): 18/18 screen tests pass on both viewports with zero console errors; the `proof` project passed at 10:16Z and timed out (60 s) at 10:38Z after the approve — timeout raised to 240 s in the config.

**Ramp files viewed (Read tool).** Bill_pay 00.26.25 + 00.29.14 · banking 00.35.26 + 00.39.39 · card_payments 00.16.30 + 00.18.08 · accounting 00.42.01 · Pay_intake 00.46.07 · landings_snapshots 21.49.15.

**Findings / for the root.** (1) `/api/today` costs 11–12 s on a fresh production DB (the view scans all SKUs); the UI is fine but the demo will feel slow until the backend caches it — I halved the calls by sharing one snapshot. (2) Owner law D-H54 applied: «Ainalym» everywhere in my shell/metadata; 24 px mark slot renders `/brand/ainalym-mark.svg` when present (hidden on 404, chartreuse-on-black fallback). (3) `image_url` thumbnail (26 px, only when present) and the «На складе ekt.kz сейчас: N (живой API|снимок от <дата>)» line are coded defensively for the pending backend fields. (4) **Merge conflict named, not resolved** (time box): main (fable_ui_2) also added `src/app/(v2)/layout.tsx`, `src/components/v2/Shell.tsx`, `shell.module.css`, `src/styles/v2/tokens.css`, `tests/ui_v2/playwright.config.ts`. Recommendation: keep this lane's shell/layout/tokens (queue badges, ⌘K, offline status, `useTodaySnapshot`), add their nav items («Позиции» → `/v2/skus/<code>`), and union the Playwright projects (`desktop`/`mobile`/`proof` + their spec). (5) Dev dependency added: `@playwright/test@1.63` (package.json/lock). (6) `.playwright/` (test output) is untracked in the worktree — not committed; add to `.gitignore` on merge. (7) Task-kind queue items have no action route (`/api/tasks` absent) — shown honestly.

Tip: see `git log lane/fable_ui_1 -1` (code commits 0de78c1 → cfa8b05 → 72bb097; this closeout is the tip). Gate: GREEN for the two screens; YELLOW on the shell merge (named above).
