# LANDING-1 closeout — public mountains landing (Opus 5.5, branch lane/landing)
- Outcome: `/landing` is public and static: full-bleed steppe-and-mountains loop over its poster, «Ainalym» + `/brand/ainalym-mark.svg`, promise «Агенты ведут цикл закупок. Решение — за вами.», «Открыть демо» → `/today` (through the demo gate), «Как это работает» anchor; sections for the four screens, the honest calculation, the assistant loop; footer «Демо для HackAlem 2026» · «Обмен с 1С — файлами». No recordings, Remotion or app frames; no `docs/evidence/shell/` screenshots exist, so none used.
- Assets (source: team Higgsfield workspace): still = Nano Banana Pro text-to-image (Kazakh steppe, Tian Shan at dawn, no people/text); loop = Kling 3.0 pro image-to-video from that still (10 s, 1080p, locked camera, drifting clouds and mist), made seamless with a 1.5 s crossfade → `public/landing/mountains.mp4` 1920×1072, 8.5 s, 3.35 MB (three other clips were generated and not used: one nearly static, two 720p); transcoded on the host with AVFoundation (no ffmpeg) to H.264 without audio, fast start; poster = the loop's first frame. No WebM (no encoder on host).
- Credits used: 53 of the 800 cap (workspace 2 547.48 → 2 494.48): still 2, Kling 12 + 15, Veo 3.1 Lite 12 + 12.
- Reference «Check of the payment on the shipping station in Kazakhstan.» NOT found: 100 latest video jobs (back to 29.04), 100 image jobs, untitled uploads, no websites, scoped local search. Design follows the owner's «mountains video and theme» (palette aligned with the Dain mountain-dark archive concept). Next route: owner drops the file or link into `docs/evidence/landing/ref/` and a follow-up lane aligns composition and palette.
- Route/allowlist: `src/middleware.ts` `PUBLIC_PATH` = `/landing`, `/landing/*`, `/brand/*`, `/icon.svg` (tab icon); `/` → `/today` unchanged.
- Checks: `npx tsc --noEmit` clean; eslint on landing + middleware clean; `npm run build` GREEN (`/landing` static); `npm run etl && npm run check` passed=267 failed=0 skipped=7 externally-unverified=7; `next start -p 3611` with the guard on: `/landing` 200 with no cookie, `/today` 307 → `/__demo_access?next=%2Ftoday`, video/poster/mark 200 (byte ranges 206); Chrome via Playwright at 1440×900 and 390×844: video playing, 0 console errors, 0 horizontal overflow; reduced motion → no video element, poster shown. Server stopped.
- Evidence: `docs/evidence/landing/landing_1440.png`, `landing_390.png` (+ `_full.jpg` full-page captures).
- Gate: YELLOW — all checks GREEN; the one open item is the owner's named reference, not located.
