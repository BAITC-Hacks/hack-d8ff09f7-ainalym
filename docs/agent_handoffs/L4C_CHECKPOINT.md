# L4c checkpoint 2 — connections and world console
done: /connections from modes + health; plain-text 1С row with verbatim meta; full world history, state/code filters, run details, play/compose, 20 s timer off by default and paused hidden.
checks: npm run build and npx tsc --noEmit passed; 20 UI state tests pass in working tree (next checkpoint).
deps: jsdom + @types/jsdom dev-only for state tests.
undone: screenshots/state commit; integrated modes/L5 routes not on main; source-file metadata and health provider fields pending owning lanes.
resumable: L4C_INTEGRATION_NOTES.md pins exact seams; L4a feed not on main, console uses shared shell controls in owned paths until reusable controls land.
Gate: RED — screenshots unrun; spoken result externally-unverified.
sha: this checkpoint commit (git log -1 --format=%H -- docs/agent_handoffs/L4C_CHECKPOINT.md).
