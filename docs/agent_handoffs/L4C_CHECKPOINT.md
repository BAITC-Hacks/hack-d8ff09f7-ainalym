# L4c checkpoint 1 — assistant mechanism
done: exported AssistantPanel, /assistant, four deterministic tools, typed path, captions seam, eight voice states, truth-labelled result cards, phone sheet.
checks: npm install; npm run build; npx tsc --noEmit passed after main merge.
undone: Связи, world console, UI state tests/screenshots, tier-2 lists; L5 spoken cards need additive seam (L4C_INTEGRATION_NOTES.md).
resumable: implement connections from /api/modes; reuse L4a WorldFeed when merged; run UI gates in step 3.
Gate: RED — UI tests/screenshots unrun; microphone externally-unverified.
sha: this checkpoint commit (git log -1 --format=%H -- docs/agent_handoffs/L4C_CHECKPOINT.md).
