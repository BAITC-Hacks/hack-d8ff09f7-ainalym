# OPUS_A closeout — branch `lane/opus_a` (D-H50, Opus 5.5), 23.09 09:52–10:12Z
- **Ramp references viewed (Read tool, 8):** `Bill_pay/CleanShot 2026-09-11 at 00.26.25@2x.png`, `Bill_pay/…00.31.02@2x.png`, `banking/…00.35.26@2x.png`, `banking/…00.37.00@2x.png`, `accounting/…00.42.01@2x.png`, `card_payments/…00.16.30@2x.png`, `Pay_intake/…00.46.07@2x.png`, `landings_snapshots/CleanShot 2026-09-10 at 21.49.15@2x.png` (all under `/Users/adil/Documents/Screenshots_m5/ramp/`).
- **Shell (all 3 screens):** own layout `src/app/(opus_a)/layout.tsx` inside `ApiProvider` (live sync via `useApiSync`/`useApi`); warm greige canvas, darker warm rail with chartreuse count badges, ⌘K SKU search (↑↓ Enter Esc), one 56 px display heading over a breadcrumb, one chartreuse primary action per page; tokens scoped to `.oa` in `src/styles/opus_a/tokens.css` + `ui.css` (global `tokens.css` untouched); Inter only.
- **Today `/opus_a/today`** — taste: Ramp Home "Requires your approval" + balance strip, so the manager reads four numbers then acts on cards, never a report. Data: `/api/today`, `/api/queue`, `/api/agent/ledger?limit=12`, `/api/money`, `GET /api/proposals/:id` (version on open) → `POST /api/proposals/:id/approve|reject {proposal_version}`. Strip: at-risk SKU, decisions, money at stake (bigint minor units; IEK «себестоимость не задана»), agent autonomy; stockout list with cover-vs-lead bars; money rail with warm alert rows; ledger feed with «Агенты · данные партнёра».
- **Replenishment `/opus_a/replenishment[?supplier=IEK|SE]`** — taste: Bills table (two-line cells, icon pills, right-aligned money, beige alert rows) because 1 259 lines need scanning, and the rationale opens in place like a receipt. Data: `/api/recommendations[?supplier=]`, `/api/skus?supplier=SE&limit=500&offset=0|500` (unit cost), `/api/skus/:code` (recommendation version), `POST /api/recommendations/:id/adjust {qty,reason,version}`. Urgency tabs with counts, filter, supplier segment, sticky supplier header with total or «себестоимость не задана», 40-row progressive render; rationale = receipt (forecast + safety − on hand − in transit = need → MOQ), seasonality bars + horizon months, excluded one-off docs, stockout months + uplift, sources, engine text, adjust form.
- **SKU card `/opus_a/skus/[code]`** — taste: Banking "cash-flow schedule + right rail", so the chart carries the story and the rail carries the decision. Data: `/api/skus/:code`, `/api/params` (lead time). 24-month bars (last 12 stronger), excluded one-off part hatched amber, stockout months shaded with red ticks, forecast dashed plum line; rail: recommendation receipt + state/version, in-transit POs with ETA + source file, sources; agent history.
- **States:** loading = shimmer skeletons at final size (no layout shift); empty = dashed panel; unavailable = red panel (e.g. `sku_not_found_phone.png`, adjust 404); stale 409 = beige «Данные обновились — обновите» + Обновить (`today_stale_409_state.png`; the real API returns 409 on a stale `proposal_version`, checked by curl).
- **Keyboard proof (`tests/e2e/opus_a.spec.ts`, 3/3 pass):** Enter on «Решить…» focuses «Подготовить заказ», Esc closes and returns focus; ↓ moves between table rows, Enter opens the rationale (≈22–25 ms), Esc from inside closes it and restores focus; ⌘K focuses search, Enter opens the SKU. Phone targets ≥ 44 px through the `.oa` media rules.
- **Screenshots (`docs/evidence/opus_a/`, 1440×900 + 390×844):** today_desktop(.png,_full), today_phone(.png,_full), today_decision_confirm, today_stale_409_state, today_search_desktop, replenishment_desktop, replenishment_se_desktop, replenishment_rationale_desktop, replenishment_phone, replenishment_rationale_phone, replenishment_adjust_state, sku_desktop(.png,_full), sku_phone(.png,_full), sku_not_found_phone.
- **Checks:** `npx tsc --noEmit` exit 0 (with and without `@playwright/test` present); `npm run build` exit 0 (routes `/opus_a/today`, `/opus_a/replenishment`, `/opus_a/skus/[code]`).
- **Deviations / gaps:** (1) `POST /api/recommendations/:id/adjust` (CONTRACTS §3, L2a) is not on `main` (404) → the UI shows the honest unavailable state and keeps qty + reason; it works unchanged once the route lands. (2) `@playwright/test` was not added to `package.json` (outside my write set); the spec carries `// @ts-nocheck` and ran with the e2e lane's Playwright 1.63 copied into untracked `node_modules`. (3) `node_modules` was an APFS clone of main's instead of `npm install` (same lockfile). (4) No approve was executed against the DB (only the confirm, 409, and 404 paths) to keep the demo state intact.
- **Commits:** 8693234 Today · a2005fb Replenishment · 1f4cb91 SKU card · d23d747 phone fixes · b4e8e6f evidence; this closeout is the tip commit. Dev server on :3110 stopped at closeout.
