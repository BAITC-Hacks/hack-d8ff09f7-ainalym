# REVIEW-FIX-1 closeout — «Проверка» /review/[id] in the Fable language (judge sim #3, defect #1)

- **Rebuilt:** `src/app/(v2)/review/[id]/ProposalReviewPage.tsx` (new client page), `review.module.css` (new), `page.tsx` (thin server wrapper, same data path), `SupplierReplyReview.tsx` (restyled on the same module; split/expedite decision and WORLD-2 labels unchanged). Nothing else touched.
- **Shows:** header «Заказ поставщику SE · 317 позиций · 73 … ₸» with a Russian state pill (ждёт решения / утверждён / отклонён / устарел); kind titles supplier_order / supplier_split / supplier_expedite / clarification (+ existing outlier_review / param_change); no `PR-` / `REC-` / `qty` anywhere in text — ids live only in `data-proposal-id` / `data-recommendation-id`.
- **«Основание»:** real table — thumbnail (image_url via `/api/skus` + `/api/recommendations`), название → SKU page, артикул, заказать with the row unit, срочность pill, цена, стоимость (KZT, thousands separators, scale classes for ≥ 1 млн / ≥ 10 тыс, «цена не задана» never a zero), one-sentence «почему» from rationale_ru expandable to the full text; sort by срочности / стоимости; «Показать ещё» past 60 rows.
- **Actions:** «Утвердить заказ» / «Отклонить» keep the same POST (`proposal_version` + `adjustments`), 409 → «Данные обновились — обновите страницу» + Обновить; «Изменить количество» opens the same inline adjust flow (edited rows tinted, «было N»); receipt «Утверждено · черновик заказа создан … предоплата 30 % X сейчас · 70 % Y <дата поставки|при поставке>» with links Деньги / Заказы / «Выгрузка для 1С» (`/api/orders/<id>/export.xlsx`). 30/70 mirrors the obligations default; the balance date is the created order's eta.
- **States:** loading skeleton + shell ribbon (apiRequest counter), 404 «Предложение не найдено», offline/unavailable via StateBlock, empty lines block, no-price warning; 390 px = card rows, no horizontal scroll (asserted).
- **Checks:** `npm run etl && npm run check` passed=313 failed=0 (7 externally-unverified); `npm run build` ok; `npx tsc --noEmit` clean; eslint clean on the lane files.
- **Evidence:** `docs/evidence/v2/reviewfix/review_desktop(.png|_full.png|_approved.png)` 1440×900 and `review_phone(.png|_full.png)` 390×844 from `tests/e2e/v2_review.spec.ts` (`--config tests/e2e/v2_review.config.ts`, `E2E_BASE_URL` against `next start`): today → first priced proposal → review, asserts no PR-/REC-/qty/state_version/JSON/HTTP/undefined/null, approve → receipt.
- **Deviations / unverified:** prepayment share is the 30 % default (supplier terms not read on this page); IEK proposals carry no priced lines, so their header/receipt show «—» honestly; supplier-reply variant restyled but not screenshotted (no reply proposal in the seeded DB).
- **Gate: GREEN**
