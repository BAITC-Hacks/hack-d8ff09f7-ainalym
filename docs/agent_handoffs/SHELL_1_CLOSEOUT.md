# SHELL-1 closeout — the Fable shell is THE shell (branch `lane/shell`, 23.09.2026)

- **Route map, before → after.** `/v2/today`, `/v2/replenishment` (FABLE-UI-1) → `/today`, `/replenishment`; `/v2/money`, `/v2/skus/[code]` (FABLE-UI-2) → `/money`, `/skus/[code]`; `/v2/supplier/[po_id]` (Fable PO desk) → `/orders/[po_id]` (the peers portal keeps `/supplier/[po_id]`, so the two could not share a path); `/v2/assistant` (ASSIST-1) → `/assistant` + dock mounted once in `src/app/(v2)/layout.tsx`; `/v2/suppliers` (SUPPLIERS-1) → `/suppliers`. New Fable-language pages: `/skus` (catalog list, `src/components/v2/SkuIndex.tsx`) and `/orders` (order list, `src/components/v2/OrdersIndex.tsx`, to be replaced when ORDERS-1 lands). Old `(app)` pages re-homed unchanged inside the Fable layout: `/world`, `/world/runs/[id]`, `/connections`, `/review`, `/review/[id]`, `/purchases` (redirect → `/replenishment`). Root `/` → `/today` unchanged. `/v2/*` and `/opus_a/*` stay as thin 307 redirects that keep query strings (`src/app/v2/[[...path]]`, `src/app/opus_a/[[...path]]`).
- **Nav (one layout, `src/components/v2/Shell.tsx`):** Сегодня · Закупки · Заказы · Поставщики · Товары · Деньги · Лента · Связи · Помощник, active state (Закупки also lights for `/purchases`, Сегодня for `/review`), brand «Ainalym» + `/brand/ainalym-mark.svg` as a link, skip link, ⌘K// search opens a SKU (9-digit code), an order (`PO-…`) or the catalog query. The dock's rail trigger reads «Спросить помощника» so it does not read as a tenth section.
- **Folded / dropped:** `/purchases` folded into Закупки (redirect kept, not in nav); `/review` kept as a route, not in nav (its decisions live on Сегодня).
- **Deleted:** `src/app/(app)` (layout + today, replenishment, skus/[code], money, orders/[id]), `src/app/(opus_a)`, `src/app/(opus_b)`, `src/components/opus_a`, `src/components/opus_b`, `src/styles/opus_a`, `src/styles/opus_b`, `tests/e2e/opus_a.spec.ts`. Old `MoneyPage` moved to `src/components/pulse/MoneyPage.tsx` (kept only for `tests/ui/a_money_page`); `components/shell/AppShell.tsx` is no longer routed (kept for `tests/ui/a_sync_shell`). Evidence folders under `docs/evidence` untouched.
- **Merged:** `lane/fable_ui_1` 945c77a (its layout/rail/tokens win; FABLE-UI-2 token names appended to the same `src/styles/v2/tokens.css`; Playwright projects united in `tests/ui_v2/playwright.config.ts`, specs re-pointed to un-prefixed paths), `lane/assist` 3de4f12, `lane/suppliers` 6d2abc5, `origin/main` e1516fe. **Not merged (not landed by closeout):** `lane/orders`, `lane/sku` — when they land: merge, `git mv src/app/(v2)/v2/<dir> src/app/(v2)/<dir>`, drop the interim `/orders` list or SKU card, sed `"/v2/` → `"/` in their files.
- **Plain-Russian sweep:** removed «Версия состояния N» footers, the «JSON» raw-data link on the SKU card and version numbers in stale banners; error states already in Russian. Untouched on purpose: contract truth labels (CONTRACTS §5) such as «Правила без LLM».
- **Checks:** `npx tsc --noEmit` clean · `npm run build` OK (all nine routes listed) · `npm run check` GREEN before the `origin/main` merge (262/0); after it the only failure was ENGINE-1's `M3 censored stockout` domain test against the pre-merge ETL database — see the final line below for the post-`npm run etl` result · `next start -p 3511`: `/`→307 `/today`; `/today /replenishment /orders /suppliers /skus /money /world /connections /assistant` all 200; `/opus_a/today`, `/v2/replenishment?supplier=SE`, `/v2/orders?supplier=IEK` → 307 with the query kept · screenshots 1440×900 in `docs/evidence/shell/` (today, orders, suppliers, skus), 0 console/page errors.
- **Gate:** GREEN for the shell; YELLOW only if the post-ETL check line below is not `failed=0`.
