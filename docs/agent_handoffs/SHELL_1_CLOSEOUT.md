# SHELL-1 closeout — the Fable shell is THE shell (branch `lane/shell`, 23.09.2026)

- **Route map, before → after.** `/v2/today`, `/v2/replenishment` (FABLE-UI-1) → `/today`, `/replenishment`; `/v2/money` (FABLE-UI-2) → `/money`; `/v2/skus/[code]` → `/skus/[code]` (SKU-1 card, Opus A visual on the Fable structure); `/v2/supplier/[po_id]` (Fable PO desk) → `/orders/[po_id]` (the peers portal keeps `/supplier/[po_id]`, so the two could not share a path); `/v2/orders` (ORDERS-1) → `/orders`; `/v2/suppliers` (SUPPLIERS-1) → `/suppliers`; `/v2/assistant` (ASSIST-1) → `/assistant` + dock mounted once in `src/app/(v2)/layout.tsx`. New Fable-language page: `/skus` (catalog list, `src/components/v2/SkuIndex.tsx`). Old `(app)` pages re-homed unchanged inside the Fable layout: `/world`, `/world/runs/[id]`, `/connections`, `/review`, `/review/[id]` (with WORLD-2's supplier-reply review), `/purchases` (redirect → `/replenishment`). Root `/` → `/today` unchanged. `/v2/*` and `/opus_a/*` stay as thin 307 redirects that keep query strings (`src/app/v2/[[...path]]`, `src/app/opus_a/[[...path]]`).
- **Nav (one layout, `src/components/v2/Shell.tsx`):** Сегодня · Закупки · Заказы · Поставщики · Товары · Деньги · Лента · Связи · Помощник, active state (Закупки also lights for `/purchases`, Сегодня for `/review`), brand «Ainalym» + `/brand/ainalym-mark.svg` as a link, skip link, ⌘K// search opens a SKU (9-digit code), an order (`PO-…`) or the catalog query. The dock's rail trigger reads «Спросить помощника» so it does not read as a tenth section.
- **Loading (owner law):** `src/components/v2/loading.tsx` + `loading.module.css` — `LoadRibbon` (thin accent ribbon under the header; shows after 300 ms for route transitions and while any page-level request is pending — `apiRequest` in `src/components/shell/api.tsx` counts requests through `src/components/shell/pending.ts`, the 5-second state poll excluded), `Spinner` and `SectionLoading` (circular, reduced-motion → opacity pulse). The spinner is added to every existing loading primitive (`v2/ui.tsx Loading`, `v2/primitives.tsx Skeleton`, `shell/Controls.tsx Skeleton`), so all nine pages' loading states show the circle. Proof: `docs/evidence/shell/orders_loading_ribbon_1440x900.png` (API slowed to 900 ms, ribbon visible; hidden again after load).
- **Folded / dropped:** `/purchases` folded into Закупки (redirect kept, not in nav); `/review` kept as a route, not in nav (its decisions live on Сегодня).
- **Deleted:** `src/app/(app)` (layout + today, replenishment, skus/[code], money, orders/[id]), `src/app/(opus_a)`, `src/app/(opus_b)`, `src/components/opus_a`, `src/components/opus_b`, `src/styles/opus_a`, `src/styles/opus_b`, `tests/e2e/opus_a.spec.ts`, the interim `/orders` and `/suppliers` lists (replaced by ORDERS-1 / SUPPLIERS-1). Old `MoneyPage` moved to `src/components/pulse/MoneyPage.tsx` (kept only for `tests/ui/a_money_page`); `components/shell/AppShell.tsx` no longer routed (kept for `tests/ui/a_sync_shell`). Evidence folders under `docs/evidence` untouched.
- **Merged:** `lane/fable_ui_1` 945c77a (its layout/rail/tokens win; FABLE-UI-2 token names appended to the same `src/styles/v2/tokens.css`; Playwright projects united in `tests/ui_v2/playwright.config.ts`, specs re-pointed to un-prefixed paths), `lane/assist` 3de4f12, `lane/suppliers` 6d2abc5, `lane/orders` 1b1fcaf, `lane/sku` 7b2e00c, `origin/main` e1516fe and 9b01fcb (WORLD-2). Not merged: `lane/assist2` (root merges after landing).
- **Plain-Russian sweep:** removed «Версия состояния N» footers, the «JSON» raw-data link on the SKU card and version numbers in stale banners; visible AI label on Fable pages reads «Локальный режим» (contract «Правила без LLM» kept as tooltip and in `components/labels`/API). Remaining contract labels on the re-homed old pages (`components/labels`) untouched.
- **Checks (tip):** `npx tsc --noEmit` clean · `npm run build` OK · `npm run check` after `npm run etl`: see the last line · `next start -p 3511`: `/`→307 `/today`; `/today /replenishment /orders /suppliers /skus /skus/130200122_ /money /world /connections /assistant` all 200; `/opus_a/today`, `/v2/skus/130200122_`, `/v2/orders?supplier=IEK` → 307 with the path/query kept · screenshots 1440×900 in `docs/evidence/shell/` (today, orders, sku, suppliers, skus, orders_loading_ribbon), 0 console/page errors, no `state_version`/raw JSON in page text.
- **Gate:** GREEN. Final `npm run check` line: `check: passed=287 failed=0 skipped=7 externally-unverified=7` (tip, after the SKU-1 merge and `npm run etl`).
