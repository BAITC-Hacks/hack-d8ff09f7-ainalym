# L2a checkpoint — quantity adjustment
Done: M1–M5 partner checks; safe world-event recompute; proposal adjustment binds new version (old approve → 409), then creates PO draft; scheduled checks, policy decisions and views built; build/TypeScript pass.
Undone: L1 ledger/queue/today routes pending, so two fail-first ledger cases are RED; strict no-float scan and L2b no-sales recompute test still conflict with lane boundaries. L3 tick uses its own scheduled check.
Resumable: L1 call `adjustRecommendation` from its route; merge main, rerun domain and API gates; root/L2b resolve two cross-lane conflicts.
SHA: this checkpoint commit (parent e18cd32).
