# SKU-1 closeout — «Товар (карточка)»: Opus A visual on the Fable card (branch `lane/sku`)

- Ported visual (Opus A → v2 tokens, no second token file, layout/Shell untouched): back-arrow crumb + inline 44 px product thumbnail beside the display title; truth chips under the title; four-cell metric strip on a card surface with dividers, 34 px numbers and small units; main/rail split 1fr + 380 px with gap 40; chart and source rows on card surfaces; rail as plain blocks with 19 px headings (Рекомендация card, Каталог ekt.kz key–value rows, В пути rows with truck icon and «ожидается», Действия агента feed with status dots and right-aligned time); recommendation card with 44 px quantity, state/cost chips, two-column receipt with bold totals, head button «Изменить количество» that opens the adjust form.
- Kept from Fable (main): sections (Продажи 24 мес, Источники и даты, Исключённые разовые документы, Рекомендация, ekt.kz, В пути, Действия агента), every number and meta string, data fetching (`useApi`), states populated/empty/unavailable/404/stale, the adjust flow (409 stale, 404 unavailable, ok), `Bars` chart with its own legend, `useRowKeys` on rail rows.
- Language law: removed the JSON button, state-version banner text, route paths, HTTP codes, provider names and z-value from the card; result codes mapped to Russian. One remaining hit «Правила без LLM» is the Shell sidebar label (outside SKU-1 write scope) — flagged for the shell owner.
- Checks: `npm run check` passed=251 failed=0 (7 externally-unverified); `npm run build` exit 0; `npx tsc --noEmit` clean; eslint on the SKU dir: 0 errors (2 pre-existing `<img>` warnings); Playwright `sku card — populated, outliers, no recommendation, in-transit, 404, unavailable, stale-409` passed, selectors unchanged.
- Test precondition: the fresh worktree DB has no recommendations until `POST /api/calc/run`; the ui_v2 suite needs a calc run (or demo reset) first. The dev server in this sandbox never hydrates any page (also `/v2/money` on main code) — tests and screenshots were taken against `next start` of the production build.
- Evidence: `docs/evidence/v2/sku_1/sku_desktop_1440x900{,_full}.png`, `sku_phone_390x844{,_full}.png` (SKU 010300095_).
- Files: `src/app/(v2)/v2/skus/[code]/SkuCard.tsx`, `src/app/(v2)/v2/skus/[code]/sku.module.css`, this closeout, evidence PNGs.
- Gate: GREEN (visual QA of the phone layout rests on the screenshot only; no owner walk yet).
