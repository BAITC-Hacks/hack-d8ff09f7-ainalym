# L4a integration findings — 2026-09-23

- At merged main 02b088e, today/queue/modes/state/agent-ledger/world routes are still absent. UI shows real unavailable states and retries; no fabricated records. Next route: merge L1/L6 checkpoints and run the replenishment Play→ledger→queue path.
- `DATABASE_PATH=data/ui-a-qa.sqlite npm run demo:reset` fails `no such table: organization`: demo_reset passes an env var, but ETL chooses its DB only via `--db`. The loader consequently wrote this worktree's default `data/partner.db` instead; neither DB is production. Root/L1 owns the fix: pass `--db databasePath` to the loader.
- Running `node scripts/etl/load.mjs --db data/ui-a-qa.sqlite` succeeds (counts only inspected), but the loader/schema currently leaves organization empty, so `/api/money` correctly returns Organization not found. Root/L1/L9 must seed the declared partner organization as part of reset. L4a does not invent organization or money records to obtain a populated screenshot.
- The existing font README and root README now both list the bundled Inter SIL OFL font; no extra root README edit was needed.
- All UI-only acceptance defects in L4A_REVIEW_CLOSEOUT.md are being resolved in L4a source with counterexample regressions; no backend/voice/other-UI-lane changes requested by these fixes.

## After merged main ed943f1 (supersedes resolved observations above)
- L6 world feed/play/compose and partner organization seed landed. The custom-DATABASE_PATH reset bug remains: demo_reset does not pass `--db` to the loader. Workaround used only for QA: `node scripts/etl/load.mjs --db data/ui-a-qa.sqlite`, then insert the unmodified 45 JSONL events into that isolated database using the same parameterised insert as demo_reset. No backend file was edited.
- Real Play: one processed canonical event, one persisted proposal, zero agent_run/agent_action records. `src/server/ledger.ts` still returns RUN-STUB/ACT-STUB identifiers, so the full ledger gate cannot pass yet. Routes today/queue/modes/state/calc/agent-ledger also remain absent.
- Adapted L4a to L6 `{rows}` feed; compose sends explicit quantity/price payloads; no unsupported manual sales/stock kinds offered. Missing result provenance/AI axes stay visibly unknown. An HTTP-200 event with state=failed now renders an error, not a successful processing receipt.
- Real browser compose: fixture in-transit +100 applied exactly once; replay did not add again. Play button retained focus through row refresh. No paid provider was called (`AI_PROVIDER=rules AINALYM_MODE=offline`).
- L4b routes are `/replenishment?run_id=` and `/orders/:id`; local links now preserve those destinations. `/skus` catalogue is absent: shell explains unavailability and links to the real replenishment table; SKU detail links remain `/skus/:code`. Remove the unavailable navigation flag when the catalogue route lands.
- Next falsifiable gate: merge L1; reset via fixed script; Play a canonical event; observe persisted ledger + queue API rows, then the same rows in Pulse without reload or moved focus. No claim of that result yet.
