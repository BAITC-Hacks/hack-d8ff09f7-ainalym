# FABLE_UI_2 closeout — `/v2/skus/[code]`, `/v2/money`, `/v2/supplier/[po_id]` (branch `lane/fable_ui_2`, 23.09.2026)

Ramp refs viewed (Read): banking/00.35.26, banking/00.37.00, Bill_pay/00.26.25, Bill_pay/00.29.14, accounting/00.42.01, card_payments/00.16.30, card_payments/00.20.33, Pay_intake/00.46.07. Language taken: greige canvas + darker sidebar with chartreuse counts, one 56 px display heading over a breadcrumb, ⌘K bar, status pills with icons, warm beige alert rows, hairlines, two-line cells, right-aligned money, muted bar/sparkline fills, black square buttons.
Shell: `src/app/(v2)/layout.tsx` (ApiProvider + `V2Shell`), tokens `src/styles/v2/tokens.css` (scoped to `.v2`), atoms/states/formatters `src/components/v2/ui.tsx`, chart `src/components/v2/Bars.tsx`. Keyboard: `/` and ⌘K focus search (Enter opens a code/PO), Esc blurs/closes, `j`/`k` walk `[data-row]` rows, ←/→ + Enter walk and pin chart months; all actions are native buttons/forms; focus ring 2 px black; phone targets ≥ 44 px.

**SKU card** — decision: Banking pattern — a KPI strip, then 24-month bars with hatched-purple excluded one-off caps, hatched-red stockout months with a marker, dashed forecast outlines, and a right rail that shows the formula (forecast + safety − on hand − in transit → MOQ rounding) before the rationale. Routes: `GET /api/skus/:code`, `POST /api/recommendations/:id/adjust` (contract-named; absent on main → explicit «Корректировка недоступна: маршрут … не реализован»), optional `image_url`/`ekt` rendered only when present. States: populated (outliers), in-transit (010500008_), no-recommendation + 8 stockout months (130200032_), 404, unavailable (503), stale-409 («Данные обновились…»), stale snapshot banner on refresh failure. Screenshots: `docs/evidence/v2/fable_ui_2/sku_{populated_outliers,in_transit,no_recommendation_stockouts,404,unavailable,stale_409}_{desktop,phone}.png`, `sku_adjust_route_missing_desktop.png`, `sku_focus_ring_desktop.png`.
**Money** — decision: four honest numbers first (cash «не заданы», committed, 60-day outflows «нет дат», stock at cost with the known-price share), then a supplier obligations table whose beige alert row names the 32 unpriced lines, then risks as warm cards and a sources block naming the tables. Routes: `GET /api/money`, `GET /api/orders` (link to the PO). States: populated, empty, unavailable (500), stale snapshot (503 on refresh, previous data retained). Screenshots: `money_{populated,empty,unavailable,stale_snapshot}_{desktop,phone}.png`.
**Supplier draft** — decision: the letter the supplier would read on the left, the 294 lines as two-line cells with rationale on Enter, and the channel as a three-step simulator on the right, labelled with the §5 strings verbatim. Routes: server `supplierChannel(po_id)` (no GET route exists; data serialised to plain objects), `GET /api/orders/:id` (live prices/version), `POST /api/supplier/:po_id/reply`, `POST /api/orders/:id/approve` (draft orders only), `GET /api/orders/:id/export.{xlsx,csv}`. States: draft, sent, confirmed, 404, stale-409. Screenshots: `supplier_{stale_409,draft,sent,confirmed,404}_{desktop,phone}.png`.

Checks: `npx tsc --noEmit` clean · `npm run build` OK (`/v2/money` static, the two dynamic) · `npm run check` passed=205 failed=0 skipped=6 externally-unverified=4 (unchanged) · Playwright `tests/ui_v2/fable_ui_2.spec.ts` 4/4 (32 PNGs; `console_errors.json` lists only the intended 404/409/500/503 responses of the tested states — no page errors). Dev-dependency added: `@playwright/test@^1.63`; no runtime deps.
Deviations/notes: `next dev` never hydrates client components in headless Chromium on this machine (main's `/money` too), so evidence runs against `next build && next start -p 3220`; the supplier test mints its own PO through calc/run → proposal approve → order approve. Nav links to `/v2/today` and `/v2/replenishment` (FABLE-UI-1) use `prefetch={false}` so they stay silent until merged. Timestamps in screenshots use the machine TZ.
Tip: see `git log lane/fable_ui_2 -1`. Gate: GREEN.
