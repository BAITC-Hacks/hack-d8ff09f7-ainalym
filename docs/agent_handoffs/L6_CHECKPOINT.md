# L6 checkpoint — merged worker gate
done: world API, judge/supplier channels, file export, /peers; partner HTTP event→recommendation→proposal verified.
done: peer check 9/9 and build GREEN; play one row processed; compose replayed; screenshots/evidence saved.
undone: full 45-event replay (WE-003 stock_snapshot fails in L2b); persisted ledger (L1 stub); /api/orders/:id/export route.
resumable: root fixes L2b payload.stocks/action mapping and L1 ledger/routes, then reruns the integration gate.
sha: HEAD (the commit containing this checkpoint).
