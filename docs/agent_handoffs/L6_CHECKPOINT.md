# L6 checkpoint — reframe integration
done: world API, judge and supplier channels, file export, /peers; event payloads aligned to L9/L2b.
done: peer check 9/9; build GREEN; partner reset 45 events, real HTTP play/compose/dedupe GREEN.
undone: full L2b/L3 worker gate, /api/orders/:id/export handoff, closeout.
resumable: merge main now, rerun world→worker→proposal and approved PO→file gates.
sha: HEAD (the commit containing this checkpoint).
