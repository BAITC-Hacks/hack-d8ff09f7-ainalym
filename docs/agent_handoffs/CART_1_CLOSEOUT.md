# CART-1 — purchase cart panel (lane/cart)

- **Outcome:** «Добавить в корзину» on /replenishment now opens a right-side sheet (420 px, full-width at 390 px, Esc/backdrop/«Продолжить подбор» close, no page shift) with the supplier's open proposal lines: thumbnail, name, артикул, editable qty (debounced draft), KZT stake-formatted sum, urgency pill, «Убрать» (draft qty 0, «Вернуть» restores); per supplier: totals (sum, prepayment 30 %/70 % from /api/params, lead time ~40/~50 дн), 2–3 plain insights (critical share, most expensive line, lines without cost), «Подготовить заказ поставщику» → /review/<proposal id>; sort «по срочности» (default) / «по сумме»; just-added line glows ~2 s and stays in view; persistent header button «Корзина · N позиций · сумма» updates live; plain-Russian empty state.
- **Mechanism:** cart quantities use the page's existing draft store (the same one «Добавить в корзину» and header «Подготовить заказ» already use — no per-line adjust API call; drafts apply through approve `adjustments`). Lists over 60 lines page with «Показать ещё».
- **Files:** `src/components/v2/CartPanel.tsx`, `src/components/v2/CartPanel.module.css`, `src/app/(v2)/replenishment/page.tsx`, `tests/e2e/v2_cart.spec.ts`, `tests/e2e/v2_cart.config.ts`, `docs/evidence/v2/cart/*.png`. Layout untouched (panel is page-local).
- **Checks:** `npm run etl` ok · `npm run check` passed=319 failed=0 skipped=7 · `npm run build` ok · `npx tsc --noEmit` clean · eslint on new files 0 errors · Playwright desktop 1440×900 + phone 390×844 pass (add line → panel with line/totals → sort toggles → Esc closes → button reopens → «Подготовить заказ» lands on /review/…; no technical terms in the panel).
- **Known gap:** quantities edited in the cart are page drafts; the /review page shows the proposal's stored quantities until «Подготовить заказ» (header) applies them.
- **Gate:** GREEN
