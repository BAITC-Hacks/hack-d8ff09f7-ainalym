# ASSIST-2 — /assistant full-width conversation surface + live voice wave — closeout

- **Built:** `src/components/assistant/AssistantSurface.tsx` + `surface.module.css` — `/v2/assistant` is now the full-width agentic surface on the Fable tokens: centred transcript (760 px) with user/assistant turns, tool results as inline `ResultCard`s (`hideTitle`), quick-action chips from `context.ts`, the mic as the primary control (64 px; idle / listening / thinking / speaking states), typed input beside it, «Включить звук» rendered only when the voice hook exposes `audioBlocked` + `enableAudio`. Keys: Enter sends, ⌘K/⌘J focus the input (capture phase, beats the shell search), Esc stops the mic.
- **Live wave (owner law):** `VoiceWave.tsx` — canvas bars in the accent, driven by Web Audio `AnalyserNode` from `local`/`remote` `MediaStream | null` props; listening = mic, speaking = remote track, thinking = slow ink pulse; reduced-motion = 4 fps, no easing. `useAssistantVoice.ts` wraps `useVoiceSession` (untouched, VOICE-FIX-1 owns `src/voice/*`), consumes `audioBlocked`/`enableAudio` and the streams. **VOICE-FIX-1 merged** (`lane/voicefix` d392e19, clean): `src/voice/useVoiceSession.ts` now also exposes `localStream`/`remoteStream` (set on `getUserMedia` / `ontrack`, cleared on release — the only change to that file), so listening follows the mic and speaking follows the assistant's track; the monitor-track fallback stays for hooks without streams.
- **Dock (other pages):** `AssistantDock.tsx` keeps the side sheet, gains a small mic + miniature live wave when a session is active, shows spoken turns; on `/assistant` it renders only the active nav link (no sheet). Conversation shared via `thread.ts` (localStorage + `storage` sync) — voice captions and voice tool results land in the same thread as inline cards.
- **Typed SKU lookup:** `src/server/assistant_answers.ts` — imports the lane's `src/server/sku_lookup.ts` resolver (trim → exact → code + «_» → prefix → article → name, Cyrillic-safe) via `resolveSkuCode`/`mentionedSku`; «почему 130200122» → `130200122_` card on any page; «почему <название>» resolves by name.
- **Checks:** `npm run etl && npm run check` → passed=299 failed=0 skipped=7 (after both merges); `npm run build` exit 0; `npx tsc --noEmit` clean; vitest `tests/ai/assistant_answers.test.ts` 8/8 (+2 resolver tests) + `tests/voice` 28/28; Playwright `tests/e2e/assist.spec.ts` 3/3 (dock, SKU dock, page surface: chip → card, ⌘K + typed «почему 130200122» → SKU card, 390 layout, dock shares the thread).
- **Evidence:** `docs/evidence/v2/assist2/01_assistant_empty_1440.png`, `02_assistant_cards_1440.png`, `03_assistant_390.png`, `04_dock_shared_thread.png`.
- **Open / unverified:** (1) at 390 px the shell shows a horizontal overflow on the assistant page in the screenshot — not measured against other pages before the deadline; verify `main` scrollWidth and fix (likely the sticky bar/chips row) — 10-min item. (2) `react-hooks/set-state-in-effect` lint hits in the new files follow the same mount-load pattern ASSIST-1 shipped (lint is not part of `npm run check`). (3) Voice states and the analyser path verified by types/build/tests only — no live provider key in this lane.
- **Gate:** GREEN — surface, resolver, shared thread, both merges, 390 px layout and all checks GREEN; live voice not exercised in this lane (no provider key).
