# SUPPLIERS_1 closeout — branch `lane/suppliers` (Fable, 23.09 10:50–11:05Z)
- **Page `/v2/suppliers`** (`src/app/(v2)/v2/suppliers/{page,SuppliersView}.tsx` + `suppliers.module.css`): Fable v2 language on main (PageHead, Kpis strip, Section, Card, Rows, Pill; warm canvas, hairlines, one chartreuse action). Root redirect 10:57Z moved it from Opus A to Fable; nothing under `(opus_a)`, `(v2)/layout.tsx` or the shell was touched.
- **Data:** new read-only `GET /api/suppliers` (`src/app/api/suppliers/route.ts`) over `src/server/suppliers_overview.ts`: `aggregateSuppliers` (pure) + `suppliersOverview` (queries `supplier`, `purchase_order`, `proposal`, `in_transit`⋈`sku`, `obligation`, `world_event kind='supplier_reply'`, plus `moneyView().committed_by_supplier`).
- **Each card shows:** lead time + review period (IEK 40/30 d, SE 50/30 d — real), open orders count + draft/approved/exported + units + waiting proposals (link `/v2/orders?supplier=`), in transit units + shipments + nearest ETA (IEK 109 517 pcs / 6 shipments / 01.11.2026; SE 50 160 pcs / 1 / 11.11.2026 — real), committed money (from `/api/money`), next payment (prepayment/balance from open obligations), terms block (currency KZT, prepayment 30 % at approval, balance 70 % at receipt from `supplier.terms`), origin (IEK — Россия; SE «не указана» because the data has no origin), last reply (newest `supplier_reply` event by `actor_id`; otherwise «ответов пока нет»). Actions: «Закупки X» → `/v2/replenishment?supplier=X`, «Заказы X» → `/v2/orders?supplier=X`.
- **Absent today (honest, not invented):** open orders 0, committed «нет», next payment hidden, replies none — this lane DB is a fresh ETL with no calc run/PO; the fields fill as soon as proposals are approved.
- **Language rule:** no state_version/JSON/HTTP/provider/ETL words on the page; the e2e asserts `main` text contains none of `state_version, JSON, HTTP, ETL, null, undefined, NaN, api/`.
- **Tests:** `tests/domain/suppliers_overview.test.ts` (2 cases: per-supplier open POs, transit sum + shipments + ETA, committed, next payment, terms 30/70, origin, latest reply; empty world) — green; `npm run check` passed=253 failed=0 skipped=7; `npx tsc --noEmit` exit 0; `npm run build` exit 0 (routes `/v2/suppliers`, `/api/suppliers`).
- **Evidence:** `tests/e2e/v2_suppliers.spec.ts` 1/1 pass against `next start` → `docs/evidence/v2/suppliers/suppliers_desktop.png` (1440×900) + `suppliers_desktop_full.png`. Run with a throwaway Playwright config (testMatch `v2_suppliers.spec.ts`, `V2_URL`), not tracked; the shared `tests/e2e/playwright.config.ts` only matches `demo.spec.ts`.
- **Deviations:** read-model lives in `src/server/` (not `src/domain/`) because the domain purity guard forbids `Number(`; counts use `parseInt`/Decimal inside; `/v2/orders` is another lane's route (links are contract-only). Servers on :3110/:3117 stopped at closeout.
- **Gate: GREEN** (unverified surface: `/v2/orders?supplier=` target page not on main yet).
