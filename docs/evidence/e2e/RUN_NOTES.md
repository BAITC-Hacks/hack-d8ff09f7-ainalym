# Run notes

2026-09-23 UTC. The hosted service changed during this session: early probes returned 404 for Today, recommendations, proposals, queue, modes, state and ledger; subsequent requests succeeded. Early assistant submissions returned 503, then later produced truth-labelled cards. These are historical observations, not claims about a pinned deployed revision. No deployment was performed by this lane.

The harness was corrected for phone-specific navigation, the hidden phone assistant heading, the duplicate world SKU labels, and response-waiter rejection handling. Initial scratch results were replaced by the final run. Empty recommendation state can be prepared using the deterministic calculation button. No automatic playback timer, email draft, voice session, reset, or external delivery was invoked.

Action budget: at most 7 typed assistant submissions across all attempts; at most 3 world compose/Play pairs. The other writes are deterministic calculation/quantity/approval actions. This is below 60 live-AI-triggering UI actions. Provider-internal usage is not exposed by the demo; no claim of exact provider token/call count is made. The suite also refuses more than 12 POST API actions per project.

Access code use is confined to filling the password form. No auth state, request body/header, trace, video, HAR or downloaded business export is retained. All inputs/textareas are masked in saved screenshots; the access-page screenshot is taken before filling. Console evidence stores error categories, not potentially sensitive message content.
