# Local demo container — checkpoints 2026-09-23 08:53–09:00 UTC

Image: `ainalym-demo` from `docker build -t ainalym-demo .` → build completed; Next compiled and typechecked. Docker context excludes local environment files. Inspection: no `.env*` file under `/app`.

Offline run: `docker run -d -e AINALYM_MODE=offline -e DATABASE_PATH=/data/ainalym.db -v ainalym-data:/data -p 3100:3000 ainalym-demo` → Docker health `healthy`. `curl -sS -i http://localhost:3100/api/health` → `HTTP/1.1 200 OK`, body `{"ok":true,"demo_guard":"off","remaining_daily_budget":null}`.

Guard run on port 3101 with a synthetic test code supplied only as a runtime environment variable: Docker health `healthy`. First `/` → 307 to code page; code page → 200, RU/EN; accepted form → 303 with signed cookie; next `/` with cookie → 200; `/api/health` → `{"ok":true,"demo_guard":"on","remaining_daily_budget":500}`. No production access code, provider key, or public demo URL was used.

Runtime rate check: 61 authenticated `/api/today` requests from one IP in one minute → 60 passed to the route, one returned HTTP 429. A clean temporary build with staged `output: "standalone"` produced `.next/standalone/server.js`; the tracked Next config stayed unchanged.

Host release shape check: clean standalone build copied into a separate temporary release; `npm ci --include=dev`, `scripts/deploy/first_etl.sh`, and `node server.js` ran locally with no provider keys. `GET /api/health` → `{"ok":true,"mode":"offline","ai_provider":"rules","providers":{"jev":"missing","openai":"missing","voice":"missing"},"db":"ok","version":1,"demo_guard":"off","remaining_daily_budget":null}`; SQLite `sku 3909`. Temporary release was removed after the check. Caddy, SSH transfer, and VPS restart remain externally unverified.

Tunnel-start shape check: `scripts/start_prod.sh` now builds from a clean temporary copy without `.env*`, passes a mounted database path to ETL, and serves on loopback. Local `GET /api/health` → HTTP 200, SQLite `sku 3909`; no provider key was supplied. This tested the app process only, not a public tunnel.

After the L9 merge, a fresh container initialized `/data/ainalym.db` with `npm run etl -- --db /data/ainalym.db` on first start. Direct volume queries: `supplier 2`, `sku 3909`, `sales_line 248915`, `sales_month 99634`, `stock_month 117282`, `in_transit 313`. Docker health `healthy`. `curl -sS -i http://localhost:3103/api/health` → `HTTP/1.1 200 OK`, body `{"ok":true,"mode":"offline","ai_provider":"rules","providers":{"jev":"missing","openai":"missing","voice":"missing"},"db":"ok","version":1,"demo_guard":"off","remaining_daily_budget":null}`. No provider keys entered the container.

Scenario gap: `POST /api/demo/example` → HTTP 404; `GET /api/today` → HTTP 404. L9 ETL and the L1 health route landed; the remaining L1 scenario routes have not. Rerun both after the next L1 floor.

Isolated pending L1 snapshot, before merging to `main`: importing its health route into middleware caused new route paths to return HTML 404; allowing `/api/health` to continue to its own handler restored route dispatch. With the required `DATABASE_PATH=/data/ainalym.db`, first-start ETL loaded `sku 3909` on the volume, but L1 `dbPath()` redirected app reads to `data/partner.db`; `POST /api/demo/example` returned HTTP 200 with `skus:0`, `recommended:0`. Diagnostic only: using `/data/demo.db` (a filename L1 accepts) returned HTTP 200 with `skus:560`, `recommended:291`. In that same diagnostic DB, `organization 0`, and `GET /api/today` returned HTTP 500 `internal_error`. The final required filename and scenario remain RED until L1 makes `dbPath()` honor the configured path and L1/L9 seeds an organization row. L1 reset also invokes the ETL without `--db`, so it needs the same path correction.

Current `main` after that isolated probe: `npm run etl -- --db ./data/ainalym.db` produced `organization 1`, `sku 3909`, `sales_line 248915`; `src/db/client.ts` currently honors `DATABASE_PATH`. The L1 branch still carries an older DB-path rewrite. The route floor needs a merge that preserves the current `main` data behavior, then a new container run with the required filename. The reset wrapper still needs `--db`.

Checks before L1 merge: `npm run check -- demo` → `passed=9 failed=0`; `npm run build` → GREEN; `npx tsc --noEmit` → GREEN. The database-path test is expected to fail when the pending L1 rewrite lands unchanged. Live provider cap is tested in isolation but awaits L3/L5 provider-boundary wiring; external phone/tunnel integration remains unverified. The merged ETL uses `xlsx`, whose installed package reports a high severity audit advisory; its supplied exports are the current input boundary.

Fresh image after the 09:44 UTC `main` merge: `docker build -q -t ainalym-demo .` → `sha256:9e2a0be5440affc728d61548d456bfc1a9a6776407b9412d001782da4fa6fcf9`. With `AINALYM_MODE=offline`, `DATABASE_PATH=/data/ainalym.db`, and a fresh named `/data` volume, Docker health became `healthy`; `/api/health` returned HTTP 200 with `mode:offline`, `db:ok`, and no configured providers. Volume counts: `organization 1`, `sku 3909`, `sales_line 248915`. `POST /api/demo/example` and `GET /api/today` both returned HTTP 404 because L1 routes remain unmerged. The running container was removed after inspection. This health body also lacks guard fields until the pending L1 health route lands.

Merged L1 route floor, 09:51–09:53 UTC: image `sha256:6ed5e9cb5f677bea6336f486d11208f20524b9ae7c332ffda99c0b0ed6f60960` built from the clean lane with the current `main` merge. Fresh volume `ainalym-l8-scenario-data`, env `AINALYM_MODE=offline`, `DATABASE_PATH=/data/ainalym.db`, published only on local port 3111. Docker health `healthy`; `GET /api/health` HTTP 200: `ok:true`, `provenance:partner_anonymised`, `ai:rules`, `mode:offline`, `demo_guard:off`, `remaining_daily_budget:null`, `db:ok`. First-start ETL reported 2 suppliers, 3,909 SKUs, 248,915 sales lines, 99,634 sales months, 117,282 stock months, and 313 in-transit rows. `POST /api/demo/example` HTTP 200: `ok:true`, 560 evaluated SKUs, 294 recommendations, one supplier-order proposal, excluded 150 SKUs for missing sales and 14 for missing stock. `GET /api/today` HTTP 200: `ok:true`, decision queue 2, stockout-risk count 141, agent actions 672. Both responses carried non-empty scenario state on the mounted database. No provider key entered the image or container.

Guard rerun on the same volume after stopping the first process, with a synthetic runtime-only access code and local port 3112: health `healthy`, `/api/health` HTTP 200 with `demo_guard:on` and `remaining_daily_budget:500`; unauthenticated `/` HTTP 307 to the RU/EN code page; code page HTTP 200; accepted form HTTP 303; the returned cookie reaches the app HTTP 200 after the app's own `/today` redirect. Both containers were removed after inspection. External HTTPS, real live provider, mobile-data access, and actual budget decrement remain unverified.
