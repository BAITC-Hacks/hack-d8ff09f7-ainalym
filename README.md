# Ainalym — Автоматический расчёт заказов поставщикам для пополнения склада

Кейс HackAlem AI (трек «Логистика», партнёр ТОО «Электрокомплект», ekt.kz). Ниже — только то, что подтверждается текущим репозиторием. Статус каждой возможности с временем проверки (UTC) — в [`docs/TASK_MAP.md`](docs/TASK_MAP.md); README не утверждает ничего сверх этой таблицы.

> Состояние README: **v1.8**, основа `main` @ `3b3dcbd`, наблюдение 2026-09-23 10:39Z; шаги разделов 7–8 пройдены на чистом клоне этого коммита без ключей. Разделы, помеченные «ожидается», описывают контракт (`docs/CONTRACTS.md`), а не работающий код; они обновляются по мере слияния веток.

## 1. Название

**Ainalym — финансовая операционная система для торгового бизнеса. Агенты ведут коммерческий цикл. Вы принимаете решения.**
*Ainalym is a financial operations OS for trading businesses. Agents run the commercial cycle. You decide.*

«Ainalym» по-казахски — «полный круг». Первая вертикаль — кейс «Автоматический расчёт заказов поставщикам для пополнения склада»: агенты считают, что и сколько заказать у каждого поставщика, по всем источникам данных компании, объясняют каждое число, а утверждает заказ только менеджер по закупу.

## 2. Проблема и для кого

**Для кого.** Менеджер отдела закупа дистрибьютора электротехники (партнёр — ТОО «Электрокомплект», поставщики IEK и System Electric, склад Алматы). Второй пользователь — финансовый руководитель: ему важно, сколько денег заказ заморозит и когда они уйдут поставщику.

**Проблема.** Сейчас потребность считается вручную в Excel, редко и не в реальном времени. Итог — излишки по одним позициям и дефицит по другим одновременно, а разовые крупные продажи (в том числе одному клиенту) раздувают «среднее» и тянут закупку вверх. Пример на данных партнёра — [`docs/FINANCIAL_EXAMPLE.md`](docs/FINANCIAL_EXAMPLE.md) (появляется вместе с документами соавтора).

**Ценность.** Меньше денег в лишнем запасе, меньше дефицита и упущенных продаж, регулярная закупка по объяснимому расчёту.

## 3. Что реализовано

Наблюдение: 2026-09-23 10:39Z, `main` @ `3b3dcbd`, чистый клон без ключей. Подтверждено кодом и проверками:

- **данные:** 12 выгрузок партнёра (`fixtures/partner/`) → SQLite командой `npm run etl` (≈ 7 с): 3 909 артикулов, 248 915 строк продаж, 313 строк товара в пути, 1 591 месяц дефицита; текущий остаток на 22.09.2026 (SE — «Свободный остаток», IEK — остаток на начало сентября минус отгрузки сентября);
- **расчёт M1–M5 на данных партнёра:** `node scripts/scenario.mjs` → 11 строк `[PASS]` (M1, M1-active, M1-source, M2, M2-peak, M3, M4, M5-full, M5, Money, World); `POST /api/calc/run` по поставщику или категории → рекомендации по каждому артикулу с обоснованием (`GET /api/recommendations`), предложение «Заказ поставщику …» в очереди (`GET /api/queue`, `/api/proposals/:id/approve|reject`);
- **движок** (`src/domain/engine.ts`): сезонность и рост, компенсация дефицита, исключение разовых документов, страховой запас, кратность, срочность, обоснование с числами; параметры поставщиков (`/api/params`);
- **заказы и деньги:** черновик заказа по поставщику, утверждение по версии, обязательства 30 % / 70 %, экспорт `GET /api/orders/:id/export.xlsx|csv` и файл для 1С (`/api/peers/onec-export/:po_id`) с ключом «Номенклатура.Код» (код 1С); деньги по поставщикам без выдуманной себестоимости для IEK (`GET /api/money`);
- **агенты и лента:** события (`/api/world/feed|play|compose`, пресеты жюри) применяются один раз; воркер пересчитывает только затронутые артикулы и пишет журнал (`/api/agent/ledger`, `/api/agent/runs`); пульс `GET /api/today`, состояние `GET /api/state`, метки `GET /api/modes`;
- **AI:** типизированные решения `jev` (TypeSafe → Vercel AI Gateway) / `openai` / `rules` / воспроизведение; черновики письма поставщику и сводки расчёта (`/api/drafts`); голос — Realtime-сессия и инструменты (`/api/voice/*`) и текстовый путь `/api/assistant/message`, работающий без ключа;
- **экраны:** «Сегодня» `/today`, «Закупки» `/replenishment`, карточка `/skus/:code`, «Проверка» `/review`, заказ `/orders/:id`, «Деньги» `/money`, «Связи» `/connections`, «Помощник» `/assistant`, лента `/world`, поставщик `/supplier/:po_id`, `/peers`;
- **хостинг-демо:** код доступа, лимиты запросов и живых вызовов (`src/middleware.ts`, `src/server/demo_guard.ts`), контейнер и скрипты развёртывания.

Проверки на 10:25Z (чистый клон `3b3dcbd`, без ключей, после `npm run etl`): `npm run check` → `check: passed=250 failed=0 skipped=7 externally-unverified=7` — 7 `UNVERIFIED`: три живых провайдера, живой черновик письма и живой каталог ekt.kz (включаются `AINALYM_LIVE_SMOKE=1` и ключами), две проверки живого голоса с микрофоном; `node scripts/scenario.mjs` → 11 × `[PASS]`.

Не подтверждено (статус — `docs/TASK_MAP.md`): живой голос с микрофоном, снимки экранов части страниц, отправка поставщику (её нет по замыслу — только черновик).

## 4. Как это работает: от входных данных до результата

Основной сценарий как поток данных:

1. **Данные** — выгрузки партнёра (история продаж по документам, помесячные продажи и остатки, товар в пути, кратность/MOQ, сезонность) → таблицы SQLite (`sales_line`, `sales_month`, `stock_month`, `in_transit`, `sku`, `season_index`).
2. **Расчёт** — менеджер запускает расчёт по поставщику или категории (или новое событие ленты — день продаж, снимок остатков, изменение товара в пути — запускает пересчёт затронутых артикулов агентом). Движок по каждому артикулу: очищает ряд от разовых заказов (M4) → восстанавливает спрос в месяцы без товара (M3) → строит прогноз с сезонностью и ростом (M2) → считает потребность за вычетом остатка и товара в пути (M1).
3. **Рекомендации** — список, сгруппированный по поставщикам, с количеством, срочностью и обоснованием по каждой строке (M5).
4. **Проверка и утверждение** — менеджер корректирует количество (исходная рекомендация сохраняется), утверждает заказ поставщику в очереди решений; утверждение привязано к версии. Автоматической отправки поставщику нет.
5. **Записи** — каждое действие агента и решение человека пишется в журнал (`agent_action`, `approval`); утверждённый заказ создаёт обязательства (предоплата 30 % при утверждении, остаток к сроку поставки).
6. **Результат** — черновик заказа поставщику, файл для 1С в форме документа «Заказ поставщику» (xlsx/csv, ключ — «Номенклатура.Код», код 1С), подготовленное письмо поставщику (не отправляется), деньги: сколько заморожено по поставщикам и когда уйдут платежи.

### Методика расчёта

Реализация — `computeNeed` в `src/domain/engine.ts` (детерминированно, количества и деньги — `decimal.js`); запуск по поставщику/категории — `runCalculation` в `src/domain/apply.ts`; параметры поставщика — `src/domain/params.ts`. Обозначения: L — срок поставки (IEK 40 дн, SE 50 дн), R — период пересмотра (30 дн).

1. **Источники (M1).** По артикулу: помесячные продажи (`sales_month`), строки документов продаж (`sales_line`, расходные накладные и события ленты), текущий остаток на 22.09.2026 (`sku.on_hand_qty`, если снимку не больше 31 дня; иначе последний известный остаток на начало месяца из `stock_month` — он обязателен), весь товар в пути (`in_transit`), сезонный индекс поставщика (`season_index`), кратность/MOQ. Если нет истории продаж у артикула, который продавался, или нет известного остатка, расчёт отказывается и называет источник. Артикул без продаж за период получает потребность 0 с пометкой «Нет продаж за период». Категория — фильтр запуска (`scope.category`).
2. **Регулярный ряд.** Строки документов группируются по (номер документа, месяц); разовые документы исключаются (алгоритм ниже). Месяц = `qty_regular`, а если его нет — число из помесячного отчёта (`qty_file`) минус исключённые документы; события ленты (дни продаж, документы жюри) добавляются к своему месяцу.
3. **Упущенный спрос (M3).** Месяц дефицита (`stockout=1`): остаток на начало 0 или неизвестен, а в двух из трёх предыдущих месяцев были продажи; отрицательный известный остаток дефицитом не считается. Такие месяцы не входят в базовый спрос: **базовый спрос = среднее по месяцам без дефицита**. Компенсация показывается отдельно: Σ max(0, базовый − продажи месяца) по месяцам дефицита.
4. **Сезонность (M2).** Индекс календарного месяца: собственный (среднее продаж месяца / базовый спрос), если у артикула ≥ 12 месяцев с продажами без дефицита; иначе индекс поставщика (выручка 2024–2025, нормирована к среднему 1).
5. **Рост (M2).** Продажи последних 6 месяцев / те же 6 месяцев год назад (месяцы дефицита заменены базовым спросом), в пределах ±50 %; если истории не хватает — 1.
6. **Прогноз.** Сумма по дням горизонта L + R: базовый спрос × рост × индекс месяца / 30; разбивка по месяцам — `monthly_forecast`.
7. **Страховой запас.** z × σ_мес / √30 × √L, σ_мес — стандартное отклонение месяцев без дефицита; z = 1,28 при уровне сервиса 90 % (1,645 — 95 %, 2,33 — 99 %).
8. **Потребность (M1).** raw = max(0, прогноз + страховой запас − остаток − товар в пути); рекомендация = raw, округлённая вверх до кратности.
9. **Срочность.** Покрытие = (остаток + в пути) / (базовый спрос × рост / 30) дней: < L — «критично», < L + R — «скоро», иначе «планово»; рекомендация 0 — «не требуется».
10. **Обоснование (M5).** `rationale_ru` с числами («регулярный спрос … шт/мес; сезонность SKU/поставщика, рост ×…; прогноз на … дн … + запас … − остаток … − в пути … = потребность … шт (кратность …); месяцы без остатка: …; исключены разовые документы: …») и `components` со всеми входами.
11. **Деньги.** Сумма заказа = Σ количество × себестоимость («СС реал», только SE); строки без себестоимости (IEK) не превращаются в 0 — считается их число («Себестоимость не задана»).

Пример (SE `300200745_`, розетка 2-я IP54 Alas Profi 54 белая, расчёт 23.09.2026): «регулярный спрос 77.242 шт/мес; сезонность SKU, рост ×0.789; прогноз на 80 дн 192.426 + запас 73.282 − остаток 23 − в пути 120 = потребность 126 шт (кратность 6)»; разбор по шагам — `docs/METHODOLOGY_RU.md` (документ соавтора).

### Алгоритм исключения выбросов

1. Единица анализа — документ продажи в месяце: строки одного документа по артикулу суммируются.
2. Порог артикула = **max(20 шт, min(3 × медиана месячных продаж, 5 × 95-й перцентиль количества в документе))**. Медиана и p95 — из `sku` (считает `npm run etl` по положительным строкам расходных накладных), иначе считаются на лету.
3. Документ с количеством > порога — **разовый крупный заказ**: он не входит в регулярный ряд, попадает в `components.outliers_excluded` (номер, месяц, количество, порог) и в текст обоснования («исключены разовые документы: …»).
4. Решение по документу фиксируется в `outlier_doc`: `excluded` — исключать, `kept` — вернуть в расчёт; пограничные документы воркер отправляет на типизированное решение (`one_off_order`) или менеджеру (`outlier_review`); в режиме «Правила без LLM» — детерминированное правило.
5. Крупные продажи одному клиенту: в выгрузках нет идентификатора клиента (только номера документов), поэтому такая продажа ловится на уровне документа.
6. Пример (эталон `oneoff`, IEK `010500008_` ВА47-29 25А): медиана месяца 14 502 шт, p95 документа 144 шт → порог max(20, min(43 506, 720)) = 720 шт; исключены 14 документов от 807 до 7 488 шт, в том числе `20000099834` на 7 488 шт; добавленный жюри документ на 5 000 шт тоже исключается, регулярный спрос меняется на 0,00 % (`node scripts/scenario.mjs`, строка M4). Если крупный документ — регулярный оптовый клиент, менеджер возвращает его в расчёт (`kept`).

## 5. Технологии

- **Node.js ≥ 24**, **Next.js 16** (App Router, route handlers), **React 19**, **TypeScript 5**.
- **SQLite** через встроенный `node:sqlite` (без внешнего сервера БД).
- **decimal.js** — деньги без чисел с плавающей точкой; **zod** — схемы API.
- **Vercel AI SDK** (`ai`, `@ai-sdk/gateway`, `@ai-sdk/openai`) — типизированные решения и черновики.
- **lucide-react** — иконки. **Vitest** — тесты. **ESLint**. **xlsx** (SheetJS, dev) — чтение выгрузок партнёра в `npm run etl`.
- Провайдер типизированных решений: `AI_PROVIDER=jev (TypeSafe → Vercel AI Gateway) | openai | rules | offline`. Без ключей — `rules` («Правила без LLM»): весь расчёт детерминирован.
- Шрифт Inter (SIL OFL) — `public/fonts/InterVariable.woff2`, лицензия `public/fonts/OFL.txt`.

## 6. Архитектура

```
fixtures/partner/*.xlsx ──npm run etl──▶ SQLite (src/db/schema.sql)
                                            │
лента событий (world_event) ──▶ воркер агентов (src/ai/worker.ts) ──▶ журнал (agent_run, agent_action)
                                            │
                     расчётный движок (src/domain/engine.ts: M4 → M3 → M2 → M1)
                                            │
      рекомендации по поставщикам (M5) ──▶ очередь решений (proposal) ──▶ утверждение человека (approval)
                                            │
      заказ (purchase_order) ──▶ обязательства (obligation) ──▶ деньги (/api/money)
                                            │
      экспорт для 1С (файл) · письмо поставщику (черновик, не отправляется) · голос (те же инструменты)
```
По контракту все записи идут через API route handlers Next.js (`src/app/api/**`), ключи провайдеров — только на сервере. Стрелки описывают контракт; что уже работает — `docs/TASK_MAP.md`.

## 7. Установка и запуск

Требуется Node.js ≥ 24 и npm. Проверено 09:48Z на чистом клоне `9bd8972` без ключей.

```bash
git clone https://github.com/BAITC-Hacks/hack-d8ff09f7-ainalym.git ainalym
cd ainalym
npm install
cp .env.example .env.local
npm run etl        # 12 файлов партнёра → data/ainalym.db (≈ 7 с)
npm run dev        # http://localhost:3000 → /today
```

База: и `npm run etl` / `npm run demo:reset`, и приложение используют один путь — `DATABASE_PATH` из окружения, по умолчанию `./data/ainalym.db` (как в `.env.example`). Если меняете `DATABASE_PATH`, меняйте его для всех команд.

Без ключей приложение работает в режиме «Правила без LLM»: `GET /api/health` → `"ai_provider":"rules"`, `"mode":"offline"`, провайдеры `missing`; метки режима видны в интерфейсе. Ключи (если есть) добавляются только в `.env.local`, никогда в репозиторий.

Команды: `npm run etl` — загрузка данных; `npm run demo:reset` — пересборка базы; `npm run check` — все проверки; `node scripts/scenario.mjs` — пять проверок ТЗ на данных партнёра; `bash scripts/clean_clone_check.sh` — проверка чистого клона.

### 7.1 Зависимости

- `next` 16.3.6 — веб-приложение и API.
- `react`, `react-dom` 19.2.8 — интерфейс.
- `ai` — Vercel AI SDK, вызовы моделей.
- `@ai-sdk/gateway` — Vercel AI Gateway (резервный путь Jev).
- `@ai-sdk/openai` — OpenAI и совместимые API.
- `zod` — схемы запросов/ответов.
- `decimal.js` — денежная арифметика.
- `lucide-react` — иконки.
- dev: `xlsx` — чтение xlsx в ETL; `typescript`, `@types/node` (24), `@types/react`, `@types/react-dom`, `vitest`, `eslint`, `eslint-config-next`.

### 7.2 Параметры окружения

Все переменные — в `.env.example`; пустое значение = безопасное значение по умолчанию.

| Переменная | Назначение | По умолчанию / без значения |
|---|---|---|
| `AINALYM_MODE` | режим приложения: `live` \| `offline` (в `.env.example` также упомянут `qualified_external`) | авто: `live` при наличии ключей, иначе офлайн |
| `AI_PROVIDER` | провайдер решений: `jev` \| `openai` \| `rules` \| `offline` | авто: `jev`, если есть `TYPESAFE_API_KEY` или `AI_GATEWAY_API_KEY`; иначе `openai`, если есть `OPENAI_API_KEY`; иначе `rules` |
| `TYPESAFE_API_KEY` | прямой ключ TypeSafe для Jev (основной путь) | нет — Jev пробует шлюз |
| `AI_GATEWAY_API_KEY` | ключ Vercel AI Gateway (`typesafe-ai/jev`), резерв после `TYPESAFE_API_KEY` | нет — Jev недоступен |
| `OPENAI_API_KEY` | OpenAI: одного этого ключа достаточно для живых решений, черновиков и голоса | нет — голос «Голос недоступен» |
| `OPENAI_MODEL` | модель OpenAI | значение по умолчанию в коде |
| `OPENAI_BASE_URL` | совместимый с OpenAI провайдер; в `.env.example` строка закомментирована | api.openai.com (пустое значение = не задано) |
| `NVIDIA_API_KEY` | необязательно: тот же OpenAI-совместимый адаптер через базовый URL | не используется |
| `DATABASE_PATH` | файл SQLite; один путь для `npm run etl`, `npm run demo:reset` и приложения | `./data/ainalym.db`; тесты — `:memory:` |
| `AINALYM_WORKER` | фоновый воркер агентов; `0` отключает интервал | включён |
| `AINALYM_FEED_AUTOPLAY` | `1` включает автовоспроизведение ленты событий | выключено |
| `DEMO_ACCESS_CODE` | код доступа к хостинг-демо | без кода |
| `DEMO_DAILY_LIVE_CALLS` | дневной лимит живых вызовов провайдера на демо | без ограничения |
| `EKT_API_USER`, `EKT_API_PASSWORD` | доступ к тестовому API каталога ekt.kz (только чтение) | нет: поля каталога пусты или из датированного снимка |

Без единого ключа весь сценарий идёт на «Правила без LLM» и помечается этой меткой; живой вызов, который не удался, показывается как «Провайдер недоступен», а не как успешный результат.

## 8. Как проверить

Без ключей, на данных партнёра, после шагов раздела 7. Ожидаемый вывод — наблюдение 10:25Z на чистом клоне `3b3dcbd`.

**Командами**

1. `npm run etl` — строки `sku: 3909`, `sales_line: 248915`, `sales_month: 99634`, `stock_month: 117282`, `in_transit: 313`, `season_index: 24`, `stockout months: 1591`.
2. `npm run etl && npm run check` (проверки данных партнёра без базы не проходят) — итоговая строка `check: passed=250 failed=0 skipped=7 externally-unverified=7` (`UNVERIFIED` — живые провайдеры и микрофон, раздел 3).
3. `node scripts/scenario.mjs` — пять проверок ТЗ на эталонных артикулах:
   - `[PASS] M1 — Товар в пути +100 уменьшает чистую потребность …` (IEK `010500006_`, Δ = 100), `[PASS] M1-active — При дефиците +100 в пути снижает потребность до кратности на 100 (SKU=010300095_, Δ=100.000)` и `[PASS] M1-source — Отсутствующий обязательный источник назван`;
   - `[PASS] M2 — Сезонный профиль SKU меняет прогноз по месяцам (max/min=10.41)` и `[PASS] M2-peak — … (peak quarter=3)` (SE `130300027_`);
   - `[PASS] M3 — Компенсация дефицита повышает расчётную потребность (raw=866.129, adjusted=1308.389)` (IEK `130200032_`);
   - `[PASS] M4 — Разовый документ 5000 шт исключён из регулярного спроса (change=0.00%, excluded=true)` (IEK `010500008_`);
   - `[PASS] M5-full — Полный расчёт SE завершён`, `[PASS] M5 — Рекомендации SE сгруппированы по поставщику и обоснованы (rows=566)`;
   - `[PASS] Money — …30/70…`, `[PASS] World — … (applied=45/45, processed=45)`.

**В приложении** (`npm run dev`, http://localhost:3000). Шаги 4, 5 и 9 пройдены через API на чистом клоне; экранные переходы — по закрытиям L4a/L4b/L6 и снимкам в `docs/evidence/`.

4. Расчёт: «Закупки» `/replenishment` → «Запустить расчёт» (или `POST /api/calc/run` с `{"scope":{"supplier":"SE"}}`) → ответ `skus: 560, recommended: 294`; в очереди — «Заказ поставщику SE: 294 позиций» (`GET /api/queue`).
5. **M1 — товар в пути виден:** карточка `/skus/300200745_` (розетка IP54 Alas Profi 54 белая, SE) → рекомендация **126 шт**, «скоро»; обоснование: «… − остаток 23 − в пути 120 = потребность 126 шт (кратность 6)». Затем лента `/world` → событие «Товар в пути +100» по коду `300200745_` (или `POST /api/world/compose` с `{"kind":"judge_message","code_1c":"300200745_","text":"Товар в пути +100","payload":{"preset":"intransit","action":"adjust_in_transit","code_1c":"300200745_","delta_qty":100}}`) → событие с меткой «Симулятор мира — синтетическое событие», агент пересчитывает только этот код → рекомендация **24 шт**, «планово», «− в пути 220»; в журнале карточки — «Пересчитана потребность 300200745_: 24 шт».
6. **M2 — сезонность:** `/skus/130300027_` (сжим У 733M) → прогноз по месяцам различается, в обосновании «сезонность SKU, рост ×0.5».
7. **M3 — дефицит:** в рекомендации `/skus/300200898_` (рамка ARTGALLERY сталь, SE) — «Без продаж из-за отсутствия остатка: 2026-03, 2026-04»; эталонный `130200032_` — строка M3 в `scenario.mjs`.
8. **M4 — разовые документы:** расчёт по IEK (`POST /api/calc/run` с `{"scope":{"supplier":"IEK"}}`) → `/skus/010500008_` → «исключены разовые документы: …, 20000099834, …» (порог 720 шт); пресет жюри «Разовый заказ 5000 шт» в ленте → документ в списке исключённых, регулярный спрос почти не меняется.
8a. **Правка количества менеджером:** `GET /api/recommendations?supplier=SE` → строка `300200745_` (`id`, `version`: 1, 126 шт) → `POST /api/recommendations/:id/adjust` с `{"qty": 132, "reason": "объект клиента сдвинулся на октябрь", "version": 1}` → `qty_recommended` остаётся 126, `qty_adjusted` = 132, состояние `adjusted`, версия 2; повтор с `"version": 1` → **409** `{"ok":false,"code":"stale_version","current_version":2}` — устаревшая версия не перезаписывает правку.
9. **M5 — заказ по поставщикам:** `GET /api/recommendations` / `/replenishment` — группы IEK и SE, у каждой строки срочность и обоснование → очередь `GET /api/queue`: «Заказ поставщику SE: 294 позиций» → текущая версия предложения — `GET /api/proposals/:id` (поле `proposal.version`; события ленты её повышают) → `POST /api/proposals/:id/approve` с `{"proposal_version": <текущая версия>}` → черновик заказа `GET /api/orders/:id` (SE, 67 651 шт, 67 449 839,07 KZT; себестоимость известна для 262 из 294 строк) → `POST /api/orders/:id/approve` с `{"version": <версия заказа>}` → экспорт `GET /api/orders/:id/export.xlsx` — файл «Заказ_поставщику_SE_<дата>.xlsx» в форме документа 1С «Заказ поставщику»: 294 строки, колонки «Номенклатура.Код», «Номенклатура», «Артикул», «Ед.», «Количество», «Цена», «Поставщик», «Дата поставки (ETA)», «Срочность», «Обоснование»; срочность по строкам: критично 58, скоро 83, планово 153 → `/supplier/:po_id` показывает «Черновик заказа — не отправлен» → `GET /api/money`: обязательства по SE — предоплата 20 234 951,72 KZT сегодня и остаток 47 214 887,35 KZT к 12.11.2026.

Вывод проверки чистого клона (`bash scripts/clean_clone_check.sh`: клон → `npm install` → `.env.local` из `.env.example` → `npm run etl` → `npm run check`) на 10:39Z: `CLEAN-CLONE: check: passed=250 failed=0 skipped=7 externally-unverified=7` → `CLEAN-CLONE: PASS @ 3b3dcbd` (локальный клон `main`). Финальный прогон против GitHub — ≈ 12:10Z, вывод будет здесь.

## 9. Данные и интеграции

Вход: стандартные отчёты 1С УТ (динамика продаж, остатки, товар в пути, MOQ) загружаются как есть, без доработки конфигурации. Выход для 1С — файл в форме документа «Заказ поставщику» (раздел 8, шаг 9); `GET /api/modes` показывает внешний контур как «Экспорт для 1С (файл)».

Данные — обезличенные выгрузки партнёра ТОО «Электрокомплект», предоставленные организатором для кейса, без изменений (`fixtures/partner/`, раскрытие — `DISCLOSURE.md`; строки, периоды и загружаемые поля по каждому файлу — [`fixtures/PROVENANCE.md`](fixtures/PROVENANCE.md)). Данных о клиентах нет — только номера документов.

| Поставщик | Файл | Содержание |
|---|---|---|
| IEK | `Динамика продаж_2025-2026.xlsx` | строки документов продаж (дата, документ, склад, количество) |
| IEK | `Ежемесячные продажи в количественном выражении за последние 2 года.xlsx` | продажи по месяцам |
| IEK | `Ежемесячные остатки продукции за последние 2 года  ИЭК.xlsx` | остатки по месяцам |
| IEK | `Путь ИЭК 22.09.2026.xlsx` | товар в пути (открытые заказы) на 22.09.2026 |
| IEK | `MOQ  ИЭК.xlsx` | минимальная партия («Мин. разр. к отгр.») |
| IEK | `Сезонность ИЭК.xlsx` | выручка поставщика по месяцам |
| SE | `Динамика продаж_Syseme Electric_2025-2026.xlsx` | строки документов продаж |
| SE | `Ежемесячные продажи в кол-м выражении SystemElectric 2024-2026.xlsx` | продажи по месяцам, кратность |
| SE | `Ежемесячные остатки SystemElectric 2024-2026.xlsx` | остатки по месяцам |
| SE | `Товар в пути_SystemElectric на 22.09.2026.xlsx` | остатки, товар в пути («СЭ в пути 24.09»), себестоимость «СС реал», «Категория 2026» |
| SE | `MOQ SystemElectric.xlsx` | кратность |
| SE | `Сезонность SystemElectric 2024-2026.xlsx` | выручка поставщика по месяцам 2024–2026 |

Объём: строки документов продаж — 171 604 (IEK) и 77 313 (SE) (пересчитано по файлам 08:52Z: непустые строки первого листа минус заголовок); продажи 2023-01 → 2026-09, помесячные остатки и продажи 2024-01 → 2026-09. Артикулов после загрузки — 3 909 (IEK 3 185, SE 724): это объединение кодов из всех 12 файлов; в строках продаж — IEK 2 151 и SE 565. Товар в пути — 313 строк (IEK 306, SE 7). Себестоимость — только SE; в файле товара в пути SE ненулевой «СЭ в пути 24.09» у 7 из 497 позиций.

Политики (заявлены, редактируемы): срок поставки IEK 40 дней, SE 50 дней; период пересмотра 30 дней; уровень сервиса 90 %; условия оплаты — предоплата 30 % при утверждении, остаток к сроку поставки; ожидаемая дата прихода товара в пути = 22.09.2026 + срок поставки (IEK 01.11.2026, SE 11.11.2026). Правила разбора: пустая ячейка продаж = 0; пустой остаток = неизвестен (не ноль); кратность/MOQ 0 или пусто = 1; себестоимость SE 0 = неизвестна. Это допущения команды, не данные партнёра.

Изображения товаров (`image_url` у артикулов и рекомендаций) — иллюстративные: миниатюры из официального каталога IEK и иллюстрации категорий; происхождение — `fixtures/PROVENANCE.md`. Каталог ekt.kz — интеграция только для чтения (`src/peers/ekt.ts`, `GET /api/ekt/status`, описание — `docs/integrations/EKT_API.md`): у артикулов и рекомендаций могут быть ссылка на товар, остаток и цена каталога (`ekt_url`, `ekt_stock_total`, `ekt_price`) с источником и датой (`ekt_source`, `ekt_as_of`): живой ответ API или датированный снимок (`scripts/ekt/snapshot.mjs` создаёт `fixtures/ekt_snapshot.json`; в репозиторий снимок не входит, поэтому в чистом клоне поля каталога пусты). Логин и пароль тестового API (`EKT_API_USER`, `EKT_API_PASSWORD`) задаются только в `.env.local` и в репозитории не хранятся. Живые запросы ограничены по времени и кэшируются, в проверках не выполняются (`UNVERIFIED` без `AINALYM_LIVE_SMOKE=1`); в расчёт потребности данные каталога не входят.

Три оси правды на каждом результате (`docs/CONTRACTS.md` §1, §5): происхождение («Данные партнёра · обезличены»), AI («Живой AI» / «Правила без LLM» / «Воспроизведение · записанное решение» / «Провайдер недоступен»), внешний контур («Экспорт для 1С (файл)»; канал поставщика — «Черновик заказа — не отправлен» / «Подтверждено (симулятор)»). События ленты, которые вводит пользователь, помечены «Симулятор мира — синтетическое событие».

## 10. Ограничения

1С: «Prior 1C sandbox access and live integration diagnostics completed; business connector unverified.» «The protected test application returned two authenticated EnterpriseData WSDLs; no SOAP or exchange operation was run.»
(Ранее выполнены доступ к песочнице 1С и диагностика живой интеграции; бизнес-коннектор не проверен. Защищённое тестовое приложение вернуло два WSDL EnterpriseData с аутентификацией; ни одна операция SOAP или обмена не выполнялась.)

Прочее:
- экспорт для 1С — файл в форме документа «Заказ поставщику» (ключ «Номенклатура.Код»); приложение не пишет в учётную систему, загрузка — штатными средствами 1С;
- заказ поставщику никогда не отправляется автоматически — только черновик после утверждения человеком; ответ поставщика — симулятор;
- себестоимость есть только у SE; суммы по IEK не считаются;
- идентификатора клиента в данных нет — разовые продажи одному клиенту ловятся на уровне документа;
- сроки поставки и условия оплаты — заявленные политики, не данные партнёра.

## 11. Ссылка на деплой

Предоставляется через платформу вместе с кодом доступа. Код доступа и ключи в репозитории не хранятся.

---

## Соответствие ТЗ

| Критерий ТЗ | Экран / маршрут | Команда проверки | Доказательство | Статус (UTC) |
|---|---|---|---|---|
| M1 Базовая потребность по всем источникам | `/skus/300200745_` + лента «Товар в пути +100» (126 → 24 шт); `POST /api/calc/run`, `POST /api/world/compose` | `node scripts/scenario.mjs` → M1, M1-source | `scripts/scenario.mjs`, `tests/domain/a_engine.test.ts`, `tests/domain/a_partner.test.ts`, `tests/fixtures/eval/replenishment_expectations.json` | PASS на данных партнёра · 09:48Z |
| M2 Сезонность и рост | `/skus/130300027_`; `GET /api/skus/:code` | `node scripts/scenario.mjs` → M2, M2-peak | то же | PASS на данных партнёра · 09:48Z |
| M3 Компенсация дефицита | `/skus/300200898_`; эталон `130200032_` | `node scripts/scenario.mjs` → M3 | то же | PASS на данных партнёра · 09:48Z |
| M4 Исключение разовых заказов | `/skus/010500008_`; лента → «Разовый заказ 5000 шт» | `node scripts/scenario.mjs` → M4 | то же + `tests/ai/partner_event.test.ts` | PASS на данных партнёра · 09:48Z |
| M5 Заказы по поставщикам + обоснование + экспорт | `/replenishment`, `/review`, `/orders/:id`; `GET /api/recommendations`, `GET /api/orders/:id/export.xlsx` | `node scripts/scenario.mjs` → M5-full, M5 | то же + `docs/evidence/peers/*` | PASS на данных партнёра · 09:48Z |
| README: методика, алгоритм выбросов, запуск | этот файл, разделы 4, 7, 8 | чистый клон по разделам 7–8 | `README.md` | пройдено · 09:48Z |
| Без автоотправки поставщику | `/supplier/:po_id` («Черновик заказа — не отправлен»); утверждение по версии в `/review` | `npm run check` → `purchase approvals and obligations …` | `tests/domain/b_money_orders.test.ts`, `docs/evidence/peers/supplier-draft.png` | PASS · 09:48Z |

## Режимы и метки

Единая таблица меток — `docs/CONTRACTS.md` §5 (её же отдаёт `GET /api/modes`). Основные: «Данные партнёра · обезличены», «Живой AI», «Правила без LLM», «Воспроизведение · записанное решение», «Провайдер недоступен», «Экспорт для 1С (файл)», «Черновик заказа — не отправлен», «Подтверждено (симулятор)», «Симулятор мира — синтетическое событие», «Голос: живой» / «Голос недоступен».

## Демо-доступ

Локально: без ключей, режим «Правила без LLM» (раздел 7). Хостинг-демо: URL, код доступа и, при необходимости, ограниченный ключ OpenAI передаются через форму платформы — не через этот репозиторий.

## Раскрытие

Что подготовлено до старта, какие AI-инструменты и сторонние компоненты используются, происхождение данных — [`DISCLOSURE.md`](DISCLOSURE.md).

## Безопасность

Правила по контракту (`docs/CONTRACTS.md` §1, §3); подтверждение в коде — `docs/TASK_MAP.md`.

- Ключи провайдеров хранятся только на сервере (`.env.local` исключён из git через `.gitignore`); для голоса браузер получает только эфемерную сессию.
- Операции ограничены списком API из `docs/CONTRACTS.md` §3; агент готовит предложения, утверждает только человек.
- Идемпотентность: события — по `(org_id, source_id)`, действия журнала — по `idempotency_key`, утверждения привязаны к версии (устаревшая версия → 409).

## EN summary

Ainalym is a financial operations OS for trading businesses; agents run the commercial cycle, you decide. First vertical: automatic supplier replenishment orders for an electrical-components distributor (HackAlem AI, Logistics track, partner Elektrokomplekt LLP, anonymised exports). As of `main` @ `3b3dcbd` (10:25Z): `npm run etl` loads the 12 partner files into SQLite; `node scripts/scenario.mjs` prints the five ТЗ checks M1–M5 plus Money and World as 11 `[PASS]` lines on partner data; the app (`npm run dev`) runs the calculation, shows recommendations with a numeric rationale per SKU, a decision queue, version-bound approval, 30/70 obligations, a 1C-compatible export and a supplier draft that is never sent. With no keys it runs in «Rules, no LLM» mode. Status per capability: `docs/TASK_MAP.md`.
